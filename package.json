{
  "name": "visual-presentation-builder",
  "private": true,
  "packageManager": "pnpm@11.5.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @repo/shared build && pnpm --parallel --filter api --filter web dev",
    "dev:api": "pnpm --filter @repo/shared build && pnpm --filter api dev",
    "dev:web": "pnpm --filter @repo/shared build && pnpm --filter web dev",
    "build": "pnpm --filter @repo/shared build && pnpm --filter api build && pnpm --filter web build",
    "typecheck": "pnpm --filter @repo/shared build && pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "db:up": "docker compose -f docker/docker-compose.yml up -d",
    "db:down": "docker compose -f docker/docker-compose.yml down",
    "db:migrate": "pnpm --filter api exec prisma migrate dev --name",
    "db:deploy": "pnpm --filter api exec prisma migrate deploy",
    "db:seed": "pnpm --filter api run seed",
    "db:studio": "pnpm --filter api exec prisma studio"
  }
}
