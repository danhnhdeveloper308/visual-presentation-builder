# PROGRESS.md — Tiến độ dự án

> Claude PHẢI đọc file này trước khi làm task, và PHẢI cập nhật lại sau khi hoàn thành task. Không xóa lịch sử cũ — chỉ chuyển mục đã xong xuống "Lịch sử hoàn thành" kèm ngày.

## Trạng thái hiện tại

**Phase đang làm:** ... + UX auth (đợt 4) + Đại tu UI (đợt 5) + **Quên/đặt lại mật khẩu qua email Brevo (2026-07-11 đợt 6) ĐÃ XONG**. Còn lại: Phase 4 (AI Assistant) + realtime collaboration + PPTX export native.
**Cập nhật lần cuối:** 2026-07-11

## Roadmap tổng quan (cập nhật 2026-07-08 theo bộ yêu cầu mới — chi tiết từng mục ở `docs/REQUIREMENTS.md`)

- [x] **Phase 0 — Scaffold:** pnpm workspace, apps/web (Next 16) + apps/api (NestJS 11) + packages/shared, Prisma + docker-compose Postgres, script `dev`/`typecheck`/`lint`/`db:*` ở root. _(xong 2026-07-07)_
- [x] **Phase 0.5 — Auth & RBAC:** register/login (argon2), Google OAuth, refresh rotation + reuse detection, Origin-check guard (CSRF), RBAC guards + seed, proxy.ts, trang login/register. _(xong 2026-07-07)_
- [x] **Phase 1 — Core Editor:** Projects CRUD + revision 409, Assets R2, Dashboard, canvas DOM + dnd-kit, 4 element, undo/redo, lưu thủ công. _(xong 2026-07-07)_
- [x] **Phase 2a — Template & Theme nền tảng:** 15 templates, 8 themes, thumbnail pipeline. _(xong 2026-07-08)_
- [x] **Phase 2b — Hoàn thiện editor (REQUIREMENTS mục I):** sidebar scroll + hover actions + lock/hide slide, 63 shapes (gradient/shadow), 400 icons + IconPicker, Header/Footer. _(xong 2026-07-08)_
- [x] **Phase 2f — Layout System (REQUIREMENTS V):** đủ **151 layout / 18 nhóm** (factory FE, thumbnail live, áp slide/thêm slide/kéo-thả/áp mọi slide) — verify 151/151 pass `presentationSchema`. _(xong 2026-07-09)_
- [x] **Phase 2e — Recycle Bin (REQUIREMENTS IV):** trang thùng rác, restore/xóa vĩnh viễn, cron 90 ngày — verify runtime đầy đủ (curl + DB thật). _(xong 2026-07-10)_
- [x] **Phase 2d (phần Theme) — Theme System (REQUIREMENTS III):** 20 system themes (giữ id 8 theme cũ), My Themes CRUD + Clone + Export/Import — verify runtime đầy đủ (curl + DB thật). _(xong 2026-07-10)_
- [x] **Phase 2d (phần Template) — Template System (REQUIREMENTS II):** 60 system templates (15 tay + 45 sinh từ 151 layout), My Templates (save-as/duplicate/rename/delete/favorite) — verify runtime đầy đủ (curl + DB thật). _(xong 2026-07-10)_
- [x] **Phase 2c — Element Table/Chart:** Chart 5 loại (bar/line/pie/donut/area, discriminated union) + Table; layout Statistics/Table + 45 template sinh từ layout đã thay bằng element thật (Media để Phase sau). _(xong 2026-07-10 — test 32/32 + verify runtime curl/DB thật)_
- [x] **Phase 3 — Trình diễn & Export:** trình chiếu fullscreen (bỏ slide `hidden`, animation theo trigger on-click, transition fade/slide/zoom + picker trong SlideInspector), export **client-side** PDF (jsPDF) / PPTX (pptxgenjs) / PNG — KHÔNG dùng Playwright server-side (xem Decision Log). _(xong 2026-07-10 — typecheck/lint/build pass; cần test tay browser)_
- [x] **Phase 3.5 — Animation System (REQUIREMENTS VI):** 21 hiệu ứng/4 nhóm, trigger/timing, Animation Pane (list/sửa/reorder/copy-paste/preview), default per-type, WAAPI. _(xong 2026-07-09; còn phát khi trình chiếu fullscreen — Phase 3)_
- [x] **Share (yêu cầu user 2026-07-10):** share với user theo email (role editor/viewer, bảng `ProjectCollaborator` có sẵn từ Phase 0) + share public bằng link (`shareToken`, trang `/p/<token>` không cần đăng nhập) — verify runtime đầy đủ curl + E2E route. _(xong 2026-07-10)_
- [ ] **Phase 4 — AI Assistant (cuối cùng):** AI sinh JSON `Presentation` từ mô tả, gợi ý layout/màu/nội dung.

## Đang làm (In Progress)

- **2026-07-10: user chốt chỉ đạo = Phase 2c rồi Phase 3 — CẢ HAI ĐÃ XONG.** Việc tiếp theo: (a) test tay browser toàn bộ (danh sách ở "Ghi chú cho session tiếp theo"); (b) Phase 4 AI Assistant (chưa có chỉ đạo chi tiết — hỏi user trước khi làm); (c) nếu user cần PDF vector text/PPTX editable thì nâng cấp export server-side (Playwright) sau.

## Yêu cầu đã chốt từ user (KHÔNG bỏ sót khi làm tiếp)

- **2026-07-08 (bộ yêu cầu lớn):** toàn bộ spec I→VI + mục tiêu "ngang PowerPoint/Canva/Gamma" đã ghi đầy đủ tại **`docs/REQUIREMENTS.md`** — đọc file đó trước khi làm Phase 2c→3.5. Điểm nhấn: 60 templates + My Templates, 20 themes + My Themes (export/import), Recycle Bin + cron 90 ngày, **151 layouts (18 nhóm)**, **Animation system per-element kiểu PowerPoint** (entrance/emphasis/exit/motion-path, trigger on-click/with-previous/after-previous, Animation Pane, default animation).
- **Shape**: đã lên 63 loại; muốn thêm nữa: extend `shapeKindSchema` (shared, CHỈ append) + hình học vào `SHAPE_CLIP_PATHS` (có helper polygon/arc/star/gear) + 1 dòng `SHAPE_OPTIONS` (`apps/web/src/lib/editor/shapes.ts`).
- **Chart: khi làm element Chart phải hỗ trợ NHIỀU loại** — tối thiểu bar/line/pie/donut/area, props theo discriminated union để mở rộng dần.

## Đã hoàn thành

- **2026-07-11 (đợt 6) — Quên/đặt lại mật khẩu qua email (Brevo) — typecheck/lint/build toàn repo pass + verify runtime curl đầy đủ trên DB thật (dọn sạch sau):**
  - **Prisma**: model `PasswordResetToken` (userId, `tokenHash` unique, expiresAt, usedAt) — chỉ lưu HASH token, raw gửi qua email (giống refresh token); migration `add_password_reset_token`.
  - **env.validation**: `BREVO_API_KEY` optional + `BREVO_SENDER_EMAIL` (default `danhnh.developer@gmail.com`) + `BREVO_SENDER_NAME` (default "Visual Builder"). **Thiếu API key → chế độ dev: log link ra console** thay vì gửi (luồng vẫn test được).
  - **MailModule/MailService** (`@Global`): gọi Brevo REST API `POST /v3/smtp/email` bằng fetch (KHÔNG thêm SDK/dependency), header `api-key`; template email HTML gradient tách ở `mail/templates.ts` (`buildResetPasswordEmail`).
  - **Shared**: `forgotPasswordSchema` (email) + `resetPasswordSchema` (token + `passwordSchema` dùng chung) + types.
  - **BE AuthService**: `forgotPassword(email)` — LUÔN trả success (chống dò email), user không tồn tại/bị khóa thì im lặng; vô hiệu token cũ chưa dùng, tạo token mới TTL **1 giờ**, gửi link `${FRONTEND_ORIGIN}/reset-password?token=...`, lỗi gửi mail chỉ log (không ném). `resetPassword(token, newPassword)` — token phải tồn tại + chưa dùng + chưa hết hạn (không thì 400), user bị khóa → 401; đặt passwordHash mới + đánh dấu token used + **thu hồi TOÀN BỘ session** trong transaction. Routes `POST /auth/forgot-password` (throttle 5/min) + `POST /auth/reset-password` (throttle 10/min), cả 2 `@Public`.
  - **FE**: hooks `useForgotPassword`/`useResetPassword` (`silentError`); trang **`/forgot-password`** (form email → màn "kiểm tra email") và **`/reset-password`** (đọc `?token=`, form mật khẩu mới + nhập lại có show/hide, màn thành công → nút đăng nhập); link **"Quên mật khẩu?"** cạnh label mật khẩu ở login form; `client.ts` thêm 2 path vào `NO_REFRESH_PATHS` (401 là lỗi nghiệp vụ, không redirect). Cả 2 trang có nền gradient + blob giống login; KHÔNG thêm vào proxy matcher (public).
  - **Verify curl**: forgot email tồn tại/lạ đều 201 (chống dò), email sai định dạng 400; reset token bừa/đã dùng → 400, password yếu → 400 zod; reset hợp lệ → login mật khẩu MỚI 201, mật khẩu CŨ 401; **session revoke đúng** (13 phiên active → 1 sau reset+login mới); token `usedAt` được set; forgot tạo đúng 1 token trong DB (Brevo nhận request không lỗi — gửi thật phụ thuộc sender đã verify trên tài khoản Brevo của user).

- **2026-07-11 (đợt 5) — Đại tu giao diện: HomePage + Dashboard full-width + bộ component loading/skeleton — typecheck/lint/build toàn repo pass:**
  - **Common components mới** (`components/ui/`): `spinner.tsx` (Spinner 4 size, thống nhất icon+màu), `skeleton.tsx` (`Skeleton` base có shimmer + `SkeletonText`/`SkeletonCard`/`SkeletonCardGrid`), `page-loader.tsx` (`PageLoader` full-trang có logo gradient + vòng quay), và **`Button` thêm prop `loading`** (tự hiện spinner + disable) + **variant `gradient`** (bỏ việc tự chèn `<Loader2>` + class gradient lặp ở mọi nút).
  - **globals.css**: bổ sung utilities còn thiếu `animation-delay-2000/4000` (blob hero/auth trước đây chạy đồng pha vì class chưa định nghĩa), keyframes `float`/`blob`/`shimmer`, helper `.bg-dot-grid`, scrollbar mảnh hiện đại, `scroll-behavior: smooth`.
  - **HomePage viết lại** (`app/page.tsx`): header glass, hero với **mockup editor dựng bằng DOM thật** (sidebar slide + canvas gradient + mini bar chart, animate-float) thay ô xám cũ; blob gradient chạy lệch pha; 6 feature card (grid 3 cột, icon gradient hover scale); section "3 bước" có mũi tên nối; CTA gradient bo tròn; footer 4 cột gọn. Nút CTA trỏ `/dashboard` hoặc `/register` theo session.
  - **Dashboard full màn hình**: container `max-w-7xl` → **`max-w-[1600px]`** (hết khoảng trống 2 bên trên màn lớn), grid project/shared thêm breakpoint **`2xl:grid-cols-5`**; hero welcome giàu hơn (blob động + 3 quick-action tile Template/Theme/Thùng rác); loading dùng skeleton grid thay 1 spinner.
  - **Áp loading/skeleton toàn app**: ProjectGrid (skeleton grid + `Button loading`), Templates/Themes/Trash (SkeletonCardGrid + grid 2xl:5), Admin (skeleton hàng bảng + PageLoader), Account/Dashboard (PageLoader). Login/Register: thêm logo gradient trên card + `Button variant=gradient loading`.
  - **Test tay**: mở `/` xem hero mockup + blob lệch pha + hover feature; `/dashboard` trên màn rộng phủ gần hết bề ngang, hover quick-action; reload các trang thấy skeleton shimmer thay vì spinner trơ; submit login/register thấy nút spinner.

- **2026-07-11 (đợt 4) — Fix login không hiện lỗi + show/hide password + trang 404 + trang tài khoản bị khóa — typecheck/lint/build toàn repo pass:**
  - **Fix login sai email/mật khẩu bị RELOAD trang không hiện lỗi**: nguyên nhân ở `lib/api/client.ts` — 401 từ `/auth/login` tuy không auto-refresh nhưng vẫn rơi vào nhánh `if (!res.ok) { if (401) onUnauthorized() }` → handler `window.location.href = "/login"` reload trang TRƯỚC khi form kịp render lỗi inline. Fix: 401 từ các path auth (`NO_REFRESH_PATHS`: login/register/refresh) là lỗi NGHIỆP VỤ → chỉ ném `ApiError` cho form hiển thị (form login/register đã có sẵn `setError("root")`), không gọi `onUnauthorized`. User bị khóa đăng nhập bằng mật khẩu giờ cũng thấy nguyên văn "Tài khoản đã bị khóa — liên hệ quản trị viên" ngay trong form.
  - **Show/hide password**: component mới `ui/password-input.tsx` (Input + nút Eye/EyeOff, nhận đủ props `<input>` nên spread `form.register()` như cũ) — áp cho form **đăng nhập**, **đăng ký**, và **3 field ở trang tài khoản** (mật khẩu hiện tại/mới/nhập lại).
  - **Trang 404** `app/not-found.tsx` (App Router bắt mọi URL không tồn tại): số 404 gradient + nút Vào Dashboard / Trang chủ.
  - **Trang `/locked`** (public, KHÔNG thêm vào proxy matcher — user bị khóa không có session): thông báo tài khoản bị khóa + nút mailto liên hệ quản trị viên (SUPERADMIN_EMAIL) + về trang đăng nhập. **BE `googleCallback`** bọc try/catch: `UnauthorizedException` (bị khóa) → redirect `${FRONTEND_ORIGIN}/locked` thay vì trả JSON 401 trần ra màn hình (đúng ca user gặp trong ảnh); lỗi khác giữ nguyên hành vi cũ.
  - **Test tay**: login sai mật khẩu → lỗi đỏ hiện trong form KHÔNG reload; nút con mắt ở 3 form; gõ URL bậy → trang 404; khóa 1 user có Google rồi đăng nhập Google bằng user đó → về `/locked`.

- **2026-07-11 (đợt 3) — Media inline playback + auto-dọn file R2 + Quota 500MB/user + Trang quản trị admin (4 yêu cầu user cùng ngày) — test 21/21 media (tổng 127) + typecheck/lint/build toàn repo pass + verify runtime curl Đầy đủ trên DB thật (dọn sạch dữ liệu test):**
  - **(1) Media phát TRỰC TIẾP trong editor (không cần trình chiếu, kiểu Canva)** — viết lại mode `editor` của `media-content.tsx`: video/audio có **nút play/pause tròn ở giữa** (điều khiển programmatic qua ref; khung media vẫn `pointer-events:none` nên kéo/chọn element không vướng, chỉ nút nhận chuột + stopPropagation); embed mặc định hiện **ảnh thumbnail YouTube thật** (`youtubeThumbnailUrl` — helper thuần có test) + nút play, bấm play mới nạp iframe (autoplay=1, tương tác trực tiếp được), nút ✕ đóng để quay lại kéo/chỉnh element. Đổi URL nguồn tự tắt iframe đang phát.
  - **(2) Preview media ĐÚNG ở sidebar editor + dashboard/trang xem** — mode `static` không còn placeholder đen: video render `<video preload="metadata">` (hiện frame đầu), embed YouTube hiện ảnh thumbnail i.ytimg.com, kèm **badge play** ở giữa; audio giữ thanh icon. Áp tự động cho slide-panel/dashboard hover preview/viewer thumbnails (đều đi qua `SlidePreview`).
  - **(3) Tự động giải phóng dung lượng khi xóa media** — `ProjectsService`: (a) `saveContent` diff `assetId` media giữa content cũ/mới → element media bị xóa (hoặc slide chứa nó bị xóa) khi Lưu sẽ dọn file; (b) `purgeProject` (xóa vĩnh viễn tay + cron 90 ngày) dọn media trong content. Hàm `cleanupOrphanMediaAssets`: CHỈ xóa khi **không còn Project/Template nào tham chiếu** (đếm bằng text-search assetId trên cột jsonb — cuid đủ đặc trưng; project nhân bản/template save-as giữ nguyên assetId nên được bảo vệ); xóa R2 lỗi chỉ log warn, chạy nền không chặn Lưu. **Verify curl**: 2 project cùng tham chiếu 1 asset → bỏ ở 1 project asset CÒN, bỏ nốt → row Asset bị xóa; permanent-delete project chứa media → asset bị dọn.
  - **(4) Quota lưu trữ 500MB/user + hiển thị** — Prisma `User.storageQuotaBytes Int @default(524288000)` + `User.lockedAt DateTime?` (migration `add_user_lock_quota`, tạo qua `prisma migrate diff` + `migrate deploy`); `GET /assets/usage` trả `{usedBytes, quotaBytes}`; **enforce ở presign** (khai gian size → confirm re-check bằng size THẬT từ HeadObject, vượt là xóa object + 400; thumbnail miễn quota để không chặn luồng Lưu); FE: **thanh quota trong dropdown profile** (dashboard) — % dùng đổi màu amber >70% / đỏ >90%, invalidate sau mỗi upload; message lỗi quota từ BE hiện nguyên văn qua toast. **Verify**: set quota 1MB → presign 2MB bị 400 kèm message; trả 500MB → 201.
  - **(5) Trang quản trị admin** — `SUPERADMIN_EMAIL = danhnh.developer@gmail.com` (shared constants): **tự thăng role admin ngay khi đăng nhập/đăng ký/Google login** (hook trong `createSession`). BE module `admin/` (guard `user:manage` — role admin có sẵn ALL_PERMISSIONS): `GET /admin/users` (kèm usage aggregate + projectCount), `PATCH /admin/users/:id/lock` (**khóa = set lockedAt + revoke TOÀN BỘ session trong transaction → mọi thiết bị văng ngay request kế; login bị chặn "Tài khoản đã bị khóa"**; không tự khóa mình 400, không khóa SUPERADMIN 400), `PATCH /admin/users/:id/quota` (zod trần 10GB). FE trang **`/admin`** (proxy matcher + FE check role, BE là chốt cuối 403): bảng user (avatar/email/role badge/projects/bar dung lượng/ngày tham gia/trạng thái), **sửa quota inline** (input MB), nút Khóa (ConfirmDialog đỏ)/Mở khóa; link "Trang quản trị" trong dropdown profile chỉ hiện với admin. **Verify curl đầy đủ**: login superadmin → role tự thành admin; user thường gọi /admin/users → 403; khóa userb → me 401 NGAY + login bị chặn + mở khóa login lại OK; tự khóa/khóa superadmin → 400; quota vượt trần → 400 zod. (Lưu ý: tài khoản superadmin trong DB local là Google-only — verify bằng cách đặt tạm passwordHash rồi TRẢ VỀ NULL sau khi test, role admin giữ lại đúng chủ đích.)

- **2026-07-11 (đợt 2) — Dashboard v2 + Toast/Confirm + Element Media (4 yêu cầu user cùng ngày) — test 17/17 mới (tổng 123) + typecheck/lint/build toàn repo pass + verify runtime curl trên DB thật (dọn sạch dữ liệu test):**
  - **(1) Dashboard redesign + hover slideshow kiểu Canva:**
    - **Nguyên nhân "project có/không có preview slide đầu"**: card chỉ hiển thị `thumbnailUrl` — thumbnail chỉ được chụp khi bấm Lưu trong editor (tính năng thêm 2026-07-08) và fail im lặng nếu ảnh R2 thiếu CORS ⇒ project cũ/chưa lưu lại/capture fail thì trống. **Fix**: `ProjectCardPreview` (components/dashboard) — thiếu thumbnail thì tự fetch content (`useProject` thêm option `enabled`, cache `['project',id]` dùng lại được khi mở editor) và render live slide đầu bằng `SlidePreview` ⇒ MỌI project đều có preview.
    - **Hover = slideshow**: hover card → fetch content → tự chuyển qua toàn bộ slide (interval 1.1s, lặp vòng), badge "i/n" + chấm tiến trình (≤12 slide), nhả hover về slide đầu/thumbnail. Dùng chung cho cả `SharedGrid` (collaborator GET được content).
    - **Header gộp profile + logout**: `ProfileMenu` dropdown (avatar + tên + chevron → email, "Tài khoản của tôi", "Đăng xuất"), đóng khi click ngoài; nav Template/Theme/Thùng rác chuyển lên header (mobile hiện chip trong hero). Hero banner gradient compact thay welcome section to; card bo `rounded-2xl`, grid 4 cột (bỏ 2xl:5) đỡ trống.
  - **(3) Dashboard tiện ích**: **tìm kiếm** theo tên (client-side) + **sắp xếp** (Sửa gần nhất/Mới tạo/Tên A→Z) + empty state riêng khi search không khớp; **đổi tên nhanh** inline trên card (icon bút chì → input, Enter/blur commit qua PATCH meta có sẵn); **nhân bản**: BE mới `POST /projects/:id/duplicate` (owner-only qua `findOwned`, title "Bản sao của …" cắt theo TITLE_MAX, `regenerateIds`, **KHÔNG copy thumbnailUrl** — key R2 gắn project gốc, preview live tự lo) + hook `useDuplicateProject` (setQueryData detail mới). Verify curl: duplicate đúng title/revision 0/id slide+element sinh lại/thumbnail null; **user B duplicate project A → 404**.
  - **(2) Sonner + ConfirmDialog:** `Toaster richColors` + `ConfirmDialogProvider` gắn ở `providers.tsx`. **Toast tập trung qua `MutationCache`** (meta convention: `successMessage` = toast thành công, `silentError` = form/dialog đã có lỗi inline — login/register/save-project/add-collaborator/change-password) ⇒ mọi mutation lỗi tự toast message ApiError, không phải rải try/catch. successMessage đã gắn cho: delete/restore/permanent/duplicate project, theme CRUD+clone, template CRUD+duplicate+save-as, collaborator add/update/remove, share link bật/tắt (toast theo trạng thái trong `useShareLink`). **Thay HẾT `window.confirm`/`window.alert`** (project-grid, trash, themes ×3, templates ×2, layout-panel "Áp mọi slide", toolbar back-chưa-lưu, export-menu ×2, useUploadImage) bằng `useConfirm` (dialog đẹp, destructive đỏ) + `toast.error`. Grep xác nhận 0 chỗ còn dùng confirm/alert trình duyệt.
  - **(4) Element Media (video/audio/embed) — REQUIREMENTS còn thiếu mục 4:**
    - **Schema shared** (additive, giữ schemaVersion 1): `mediaElementSchema` — `kind` video/audio/embed, `url` (URL hoặc `""` placeholder), `assetId?`, `posterUrl?`, `autoplay/loop/muted/controls?`, `borderRadius?`; LIMITS mới `MEDIA_MAX_BYTES` 100MB + `MEDIA_ALLOWED_MIME` (mp4/webm/mp3/wav/ogg); `presignAssetSchema` nhận mime media với giới hạn dung lượng THEO LOẠI (`maxBytesForMime` dùng chung FE-BE, BE confirm cũng check theo mime thật từ HeadObject); `MIME_EXT` BE thêm 5 ext.
    - **Render 3 chế độ** (`media-content.tsx` dùng chung): `editor` = hiển thị thật nhưng pointer-events none (kéo/chọn không vướng); `static` (thumbnail/preview/export) = placeholder nhẹ, KHÔNG nhúng iframe hàng loạt; `interactive` (trình chiếu + trang xem) = video/audio/iframe phát thật, `stopPropagation` để click media không chuyển slide, bật lại pointer-events trong khung `SlideStatic` vốn pointer-events-none. `SlideStatic`/`SlidePreview` thêm prop `interactiveMedia` — Presenter + slide lớn của PresentationViewer truyền true.
    - **UX**: toolbar menu "Media" (Video/Âm thanh/Nhúng YouTube-Vimeo) chèn placeholder; `MediaInspector` (inspector-media.tsx): URL input (**embed tự chuẩn hoá link watch/shorts/youtu.be/vimeo → URL nhúng qua `toEmbedUrl`** — hàm thuần `lib/editor/media.ts` có test), upload file video/audio lên R2 (`useUploadMedia` — presign→PUT→confirm, không cần width/height), toggles controls/autoplay/loop/muted, bo góc; animation pane nhận type media (label + default animation); `applyTheme` bỏ qua media.
    - **Test** `scripts/test-media.ts` 17/17: schema hợp lệ/kind lạ/url rác, factory (16:9 video, thanh audio, zIndex nối tiếp), toEmbedUrl 10 dạng URL. **Verify runtime curl**: save content 4 element media → 200 round-trip nguyên vẹn; kind lạ + url rác → 400, revision không đổi; presign mp4 50MB OK/.mp4, mp4 150MB → 400, png 20MB → 400, mime lạ → 400, mp3 → .mp3.
    - **Giới hạn ghi chú**: export PDF/PPTX/PNG và thumbnail render media dạng placeholder tĩnh (html-to-image không chụp được video/iframe); nhóm layout Media (6) vẫn dùng mockup shape — có thể thay bằng element media thật ở session sau (việc nhỏ).

- **2026-07-11 — Trang tài khoản + Editor tiện ích (user chốt "làm theo đề xuất" — 2 mục ưu tiên đầu của danh sách rà soát) — test 10/10 mới (tổng 106) + typecheck/lint/build pass + verify BE bằng curl đầy đủ:**
  - **Trang `/account`** (proxy bảo vệ; link avatar+tên ở header dashboard):
    - **Hồ sơ**: đổi tên + upload avatar (tái dùng pipeline presign R2 `useUploadImage` → PATCH `/users/me`); email không đổi được (định danh). `AuthUser` thêm **`hasPassword`** (false = tài khoản Google chưa đặt mật khẩu).
    - **Mật khẩu**: đổi (bắt buộc mật khẩu hiện tại, verify argon2, sai → 401) hoặc **đặt lần đầu cho tài khoản Google** (không cần current); quy tắc mật khẩu tách `passwordSchema` dùng chung với đăng ký; **đổi xong thu hồi MỌI phiên khác** (trả `revokedOtherSessions`).
    - **Phiên đăng nhập**: `GET /auth/sessions` (đánh dấu phiên hiện tại qua `sessionId` có sẵn trong JWT), thu hồi từng phiên `DELETE /auth/sessions/:id`, **"Đăng xuất mọi thiết bị khác"** `POST /auth/sessions/revoke-others`. AuthGuard check `revokedAt` trong DB mỗi request → thiết bị bị thu hồi **đăng xuất NGAY request kế tiếp, không cần socket** (đây là lý do không làm socket `session:revoked`). UI parse nhanh user-agent (browser · OS) + IP + thời điểm đăng nhập.
    - **BE**: module `users/` mới (PATCH `/users/me`, POST `/users/me/password` — self-service, không cần permission riêng) + 3 route sessions trong `auth.controller`. **Verify curl**: 2 login = 2 phiên + cờ current đúng; đổi tên OK; sai mật khẩu hiện tại 401; đổi đúng → phiên kia 401 ngay; login bằng mật khẩu mới OK; thu hồi 1 phiên → phiên đó 401; id bịa 404; **user khác không thu hồi được phiên của mình (404)**; revoke-others đúng số lượng. (Dữ liệu test đã trả về nguyên trạng.)
  - **Editor tiện ích**:
    - **Ctrl+C/X/V**: copy/cut element vào clipboard NỘI BỘ (`useEditorUiStore.elementClipboard`) → **dán sang slide khác được**; dán = id mới + groupId remap giữ liên kết nhóm + lệch +24 + zIndex nối tiếp trên slide đích + tự chọn bản dán (logic thuần `cloneElementsForPaste` trong `lib/editor/arrange.ts`); không chặn copy text hệ thống (chỉ preempt khi đang chọn element, guard contentEditable/input có sẵn).
    - **Căn hàng & phân bố đều** (`lib/editor/arrange.ts` thuần + test): 6 nút căn (trái/giữa-ngang/phải/trên/giữa-dọc/dưới) — **chọn 1 element căn theo SLIDE, chọn nhiều căn theo khung bao selection**; ≥3 element có "Đều ngang/Đều dọc" (giữ 2 phần tử ngoài cùng, chia đều KHOẢNG HỞ như PowerPoint). UI ở cả `MultiSelectInspector` lẫn `CommonInspector` (chọn đơn); undo được, box đã đúng chỗ không bị đánh dirty thừa.
    - **Khóa tỉ lệ khi resize**: kéo handle GÓC của ảnh giữ nguyên tỉ lệ mặc định (Shift = tự do); element khác giữ Shift = khóa tỉ lệ — neo lại đúng cạnh đối diện với handle w/n.

- **2026-07-10 — Share với user + Share public (yêu cầu user) — typecheck/lint/build pass + verify runtime curl đầy đủ + E2E smoke route:**
  - **Prisma**: `Project.shareToken String? @unique` (migration `add_share_token` tạo qua `prisma migrate diff` + `migrate deploy` vì môi trường non-interactive); bảng `ProjectCollaborator` (đã có sẵn từ Phase 0) chính thức được dùng.
  - **Shared**: `schemas/share.ts` — `addCollaboratorSchema`/`updateCollaboratorSchema` (role editor/viewer), types `CollaboratorDto`/`SharedProjectSummary`/`ShareInfo`/`PublicPresentationDto`; `ProjectDetail` thêm `myRole` (owner/editor/viewer) + `shareToken` (**chỉ owner thấy**, người khác luôn null).
  - **BE — hệ quyền 3 cấp trong `ProjectsService`**: `findAccessible(userId, id, minRole)` — owner > editor > viewer; **không có quyền → 404** (chống IDOR, không lộ tồn tại); **viewer đụng thao tác editor → 403**. `get` = viewer+; `saveContent`/`updateMeta` = editor+ (giữ nguyên optimistic revision 409); delete/restore/permanent/trash/share = owner-only. Collaborators: list (mọi người có quyền xem), add theo email (owner, upsert đổi role, chặn tự thêm mình 400, email lạ 404), update role, remove (owner gỡ bất kỳ ai; collaborator TỰ RỜI được). Share public: `enableShare` sinh token `randomBytes(18).base64url` (bật lại giữ token cũ — link ổn định), `disableShare` null (link chết ngay), `getPublicByToken` không lộ thông tin owner.
  - **Routes**: `GET /projects/shared-with-me` (đặt TRƯỚC `:id` như trash), CRUD `/projects/:id/collaborators(/:userId)`, `POST|DELETE /projects/:id/share`; **`PublicController`** mới: `GET /public/presentations/:token` `@Public()` + throttle 60/min (OriginGuard bỏ qua GET nên không vướng CSRF).
  - **FE**: `ShareDialog` (nút "Chia sẻ" toolbar — CHỈ owner): thêm user theo email + đổi role + gỡ, toggle link public + copy URL `/p/<token>`; hooks `useCollaborators`/`useSharedProjects`/`usePublicPresentation` + mutations (share link sync thẳng cache `['project',id]`). **`PresentationViewer`** dùng chung: trang **`/p/[token]`** (public, ẨN slide hidden) và **`/view/[id]`** (đăng nhập, collaborator viewer — hiện đủ slide kèm badge "ẩn", editor/owner có link "Mở trong editor"); slide lớn + dải thumbnail + nút Trình chiếu (tái dùng `Presenter`). Dashboard thêm mục **"Được chia sẻ với tôi"** (badge Chỉnh sửa/Chỉ xem, editor → editor, viewer → /view). Editor: `myRole === "viewer"` tự redirect sang `/view/:id`; role truyền vào Toolbar. `proxy.ts`: thêm `/view/:path*` (bảo vệ), **KHÔNG thêm `/p`** (public).
  - **Verify runtime** (API 3101 + curl, 2 user thật, dọn sạch sau): B chưa share GET → 404; A thêm B viewer → B thấy trong shared-with-me, GET được (myRole=viewer, shareToken=null), PUT content/PATCH → **403**; nâng editor → PUT 200 revision+1 nhưng bật share/xóa project → 404; A bật share → xem public KHÔNG cookie 200 (chỉ title/content/updatedAt), bật lần 2 giữ nguyên token, tắt → 404, token bịa → 404; B tự rời → GET 404; B thêm collaborator hộ → 404; email lạ 404 / tự thêm mình 400. **E2E route**: `/p/<token thật>` không cookie → 200; `/view/:id` không cookie → 307 login.

- **2026-07-10 — Feedback đợt 3: lùi trình chiếu chuẩn PowerPoint + 44 hiệu ứng — test 51/51 (+32+13 cũ), typecheck/lint/build pass:**
  - **Lùi từng bước sau khi quay lại slide (fix đúng logic PowerPoint)**: trước đây lùi về slide trước chỉ hiện trạng thái build-xong nhưng lịch sử bước TRỐNG → không lùi tiếp từng hiệu ứng được. Fix trong `createPresentationPlayer`: option `startAtEnd` giờ **phát lại toàn bộ chuỗi ở chế độ tức thời (WAAPI `finish()`)** theo đúng cấu trúc chain (mỗi click = 1 chain gồm bước on-click + các after/with-previous đi kèm) → lịch sử `played` đầy đủ: **lùi gỡ đúng 1 bước/lần theo thứ tự ngược, tiến lại replay hiệu ứng thật**. Pre-hide entrance áp CẢ khi startAtEnd (trạng thái hiện đến từ fill-forwards của animation đã finish — hủy là ẩn lại đúng). Exit đã phát giữ trạng thái ẩn ở "final state" như PowerPoint.
  - **44 hiệu ứng (từ 21) khớp bộ PowerPoint** — enum append-only, content cũ không vỡ: Entrance 16 (+Drop, Flip, Swivel, Stretch, Shape-circle, Grow & Turn), Emphasis 11 (+Teeter, Color Pulse, **Desaturate/Darken/Lighten/Transparency GIỮ trạng thái tới hết slide** — fill forwards riêng 4 effect này), Exit 13 (+Disappear, Float Out, Split, Bounce, Spin Out, Stretch, Shape, Flip — đối xứng đủ với entrance), Motion 4 (+**Arc** bezier lấy mẫu 13 keyframes, **Turn** gấp khúc L, **Circle** khép kín về chỗ cũ). Flip/Swivel dùng `perspective(900px) rotateY`; Shape dùng `clip-path: circle()`; đường cong sample bằng hàm thuần `samplePath/arcPath`. AnimationPanel tự nhận đủ 44 (render từ `ANIMATION_EFFECTS`).
  - **Export không kèm animation** — ghi rõ vào UI menu Xuất ("file là ảnh tĩnh, hiệu ứng chỉ chạy khi Trình chiếu"): PPTX xuất dạng ảnh-mỗi-slide nên KHÔNG THỂ mang animation (pptxgenjs không hỗ trợ ghi animation; muốn có thật phải map element sang shape/text native PPTX + tự sinh XML `p:timing` — dự án lớn riêng, xem "Vấn đề tồn đọng").
  - Test mới `scripts/test-animations.ts` 51/51: enum=meta 1-1, keyframes mọi effect hợp lệ ở rotation 0/30 (giữ `rotate()`), offset tăng dần, fill đúng nhóm, motion-circle khép kín, motion-arc đúng đích, autoReverse ×2+alternate, cấu trúc chain (on-click×n → n chuỗi; after/with-previous dính chuỗi trước).

- **2026-07-10 — 3 fix theo feedback user đợt 2 (table UX + export trắng + trình chiếu) — test 13/13 + 32/32, typecheck/lint/build pass, verify schema runtime bằng curl:**
  - **Table nâng cấp** (`lib/editor/table.ts` hàm thuần + test 13/13; schema thêm `cellStyles?: (TableCellStyle|null)[][]` additive — verify runtime: save 200, round-trip nguyên vẹn, bg=number → 400):
    - **Chọn ô/vùng bằng chuột**: nhấn đúp vào bảng chọn luôn ô dưới con trỏ; click chọn 1 ô, **kéo chuột qua các ô chọn vùng chữ nhật** (anchor→focus kéo hướng nào cũng được, highlight tím + tắt bôi đen text khi multi-select); selection nằm ở `useEditorUiStore.tableSelection` cho Inspector đọc, tự dọn khi bỏ chọn element.
    - **Thêm/xóa hàng-cột theo VỊ TRÍ chọn**: chèn hàng dưới / cột phải vùng chọn, xóa đúng các hàng/cột trong vùng (label nút hiện "hàng 2–3"…); không có selection thì fallback cuối bảng như cũ; `cellStyles` + `columnWidths` đồng bộ khi chèn/xóa; selection tự kẹp theo kích thước mới; chặn xóa toàn bộ (luôn giữ ≥1 hàng/cột).
    - **Tô màu từng ô/hàng/cột**: chọn vùng → Inspector hiện "Định dạng ô đang chọn" (nền ô + chữ ô + nút xóa định dạng vùng); render đè theo thứ tự cellStyle > header/zebra > style chung, ma trận toàn null tự thu về `undefined` cho gọn JSON.
  - **Fix export trắng toàn bộ slide**: html-to-image **clone kèm computed style của chính node được chụp** — chụp thẳng wrapper `position:fixed; left:-99999px` làm nội dung clone bị đẩy ra ngoài khung → ảnh trắng. Fix: wrapper off-screen chỉ làm CHA, node chụp là div con `position:relative` 1280×720; thêm `document.fonts.ready` + `backgroundColor` trắng. **Thumbnail trong `editor-shell.tsx` dính đúng lỗi này** (chụp wrapper fixed) — fix cùng pattern.
  - **Fix trình chiếu hiện hết element ngay khi mở**: render đầu tiên `scale=0` nên `SlideStatic` CHƯA mount → player được tạo trên DOM rỗng → pre-hide entrance không có tác dụng, click mới thấy hiệu ứng chạy trên element đã hiện sẵn. Fix: đo viewport bằng `useLayoutEffect` + effect tạo player **gate theo `ready` (scale>0)** và cũng là layout effect → pre-hide xảy ra TRƯỚC paint, không chớp hình.
  - **Tiến/lùi chuẩn PowerPoint**: player thêm **`prev()`** — lịch sử theo chuỗi đã phát (mỗi click = 1 chain gồm bước on-click + các after-previous nối sau), lùi = cancel toàn bộ Animation của chain gần nhất (WAAPI cancel → entrance ẩn lại nhờ inline opacity 0, exit hiện lại) + reset index, có cờ `aborted` chặn chain đang phát dở tiếp tục từ index đã reset; **lùi về slide trước hiện trạng thái ĐÃ build xong** (option `startAtEnd`: không pre-hide, không tự phát, không transition); chuột phải/←/PageUp/Backspace đều lùi từng bước; End = nhảy tới slide cuối ở trạng thái build đủ.
  - Lưu ý vận hành trong session: 1 lệnh build chạy nhầm cwd sang repo HRM (đúng cảnh báo sẵn có trong PROGRESS) → phát hiện nhờ test fail vì dist thiếu schema mới, đã build lại đúng repo + `git checkout` trả `tsconfig.tsbuildinfo` của HRM về nguyên trạng.

- **2026-07-10 — Phase 3: Trình chiếu fullscreen + Export PDF/PPTX/PNG — typecheck/lint/build toàn repo pass (CHƯA test tay browser):**
  - **Trình chiếu** (`components/editor/presenter.tsx`, nút "Trình chiếu" toolbar + state `presenting` ở `useEditorUiStore`): overlay fullscreen (requestFullscreen best-effort — bị chặn vẫn chạy overlay), bắt đầu từ slide đang chọn, **bỏ qua slide `hidden`** nhưng đánh số trang theo vị trí GỐC; scale fit màn hình (letterbox đen); điều khiển: click/Space/→/PageDown tiến, chuột phải/←/PageUp/Backspace lùi, Home/End, Esc thoát (thoát fullscreen của browser cũng thoát trình chiếu); click sau slide cuối = kết thúc. Editor keydown handler `return` sớm khi `presenting` (không nuốt Delete/mũi tên).
  - **Animation theo trigger khi trình chiếu**: thêm `createPresentationPlayer` vào `lib/editor/animations.ts` (KHÁC preview auto-advance): with-previous gộp bước, after-previous tự phát nối tiếp, **on-click ĐỢI click** — click phát bước kế + chuỗi after-previous theo sau; hết bước thì click mới sang slide; entrance pre-hide, cancel khi rời slide. `SlideStatic`/`StaticElement` gắn `data-el-id` để player tìm node (trước đây chỉ canvas editor có).
  - **Transition**: chạy khi chuyển TỚI slide có `transition` (fade/slide/zoom, WAAPI 350ms ease-out trên frame). **SlideInspector thêm select "Chuyển slide"** (Không/Fade/Slide/Zoom) — trước đây schema có field nhưng CHƯA có UI đặt.
  - **Export client-side** (`components/editor/export-menu.tsx`, menu "Xuất" toolbar; deps mới `jspdf` + `pptxgenjs`, đều dynamic import để không phình bundle editor): render ẩn từng slide 1280×720 bằng `SlideStatic` (tách từ `SlidePreview`, nhận `scale` trực tiếp) → chụp PNG **pixelRatio 2** qua html-to-image (đợi `<img>` load xong mới chụp, flushSync để DOM commit ngay); **PDF** = jsPDF landscape 1280×720/trang; **PPTX** = pptxgenjs layout 16:9, mỗi slide 1 ảnh full-slide; **PNG** = slide đang xem. Slide ẩn bị bỏ qua (PNG thì xuất đúng slide đang xem kể cả ẩn). Lỗi (thường do CORS R2) → alert hướng dẫn. Tên file sanitize từ title.
  - **pnpm-workspace.yaml**: `core-js: false` trong allowBuilds (postinstall chỉ in banner; pnpm 11 tự chèn placeholder khi cài pptxgenjs — không chốt là `pnpm install` exit 1 làm fail mọi lệnh build sau đó).
  - Test 32/32 (charts) vẫn pass; typecheck + lint + build toàn repo pass. **Chưa test tay browser** — WAAPI/fullscreen/export cần chạy thật.

- **2026-07-10 — Phase 2c: Element Table + Chart (5 loại) — test 32/32 + typecheck/lint/build toàn repo pass + verify runtime curl trên DB thật:**
  - **Schema shared** (additive, giữ `schemaVersion: 1`): `tableElementSchema` (`rows: string[][]` + refine mọi hàng cùng số cột, `headerRow`, `columnWidths` fr, `style` {headerBg/headerColor/cellBg/zebraBg/textColor/borderColor/fontSize}); `chartElementSchema` với **props là discriminated union theo `chartType`** — bar (stacked/horizontal), line (smooth/showDots), area (smooth), pie, donut (innerRadiusRatio 0.2–0.9); axis-chart dùng `labels + series[{name,color?,values}]`, pie/donut dùng `slices[{label,value,color?}]`; LIMITS mới TABLE_ROWS/COLS/CELL_MAX, CHART_CATEGORIES/SERIES/SLICES/NAME_MAX. BE tự validate qua presentationSchema — không cần sửa API.
  - **Render**: `lib/editor/charts.ts` (hàm THUẦN có test: niceTicks 1/2/5×10ⁿ luôn chứa 0, formatChartValue K/M, linePath/areaPath smooth bezier, pieSlices arc kể cả donut đục lỗ + lát 100% không NaN, axisDomain gồm stacked ± , convertChartType giữ dữ liệu giữa 5 loại, palette 8 màu fallback theo index). `chart-content.tsx` SVG thuần (viewBox theo size logic — zoom không vỡ; grid + tick + legend + giá trị; bar âm/dương baseline 0). `table-content.tsx` CSS grid (cột fr, hàng 1fr, header đậm, zebra theo hàng DỮ LIỆU). Cả hai dùng CHUNG editor (`element-view.tsx`) + preview (`slide-preview.tsx`) → thumbnail/template preview khớp 100%.
  - **UX editor**: toolbar thêm nút **Bảng** + menu **Biểu đồ** (5 loại, data mẫu từ `defaultChartProps`); **nhấn đúp vào bảng để sửa ô ngay trên canvas** (contentEditable từng ô, Escape blur, thoát khi bỏ chọn; `e.stopPropagation` trong ô để Delete/Ctrl+Z không nuốt); `TableInspector` (±hàng/cột, header toggle, zebra, màu, cỡ chữ) + `ChartInspector` (đổi loại GIỮ dữ liệu qua `convertChartType`, toggles legend/values/stacked/ngang/smooth/dots, slider lỗ donut, **editor dữ liệu**: sửa nhãn/giá trị/màu từng chuỗi & lát, thêm/xóa chuỗi-nhóm-lát có chặn min/max) — file riêng `inspector-data.tsx`.
  - **Tích hợp**: `applyTheme` — table lấy `headerBg=accent`, chart chỉ đổi `textColor` theo body (KHÔNG đụng màu dữ liệu user chọn); animation pane thêm label/type Bảng & Biểu đồ (default animation per-type hoạt động); nhân bản/nhóm/marquee/alignment tự hoạt động (dùng element base chung).
  - **Layout**: nhóm Statistics 9 — 6 layout dùng **chart thật** (kpi-dashboard/pie-summary/line/bar/area/donut; big-number/metrics-grid/progress-bars giữ shape vì đẹp sẵn); nhóm Table 6 — **cả 6 dùng bảng thật** (simple/zebra/comparison/pricing/schedule/calendar). Media (6) giữ mockup — cần element video/audio (ngoài phạm vi 2c).
  - **45 template sinh từ layout**: script sinh gốc đã xóa (mất recipe) → viết script 1-lần nhận diện slide placeholder cũ bằng **fingerprint text đặc trưng** (+ phân biệt simple/zebra bằng màu card header cũ; CHỈ thay slide chưa có chart/table → idempotent; assert không va chạm với 139 layout khác) — **thay đúng 20 slide** trong `seed-templates-generated.ts`, validate schema từng template, chạy lại lần 2 thay 0 slide. Script đã xóa sau khi dùng (đúng tiền lệ).
  - **Verify runtime** (boot API port 3101 tránh đụng dev server đang chạy của user; DB Postgres thật; dọn sạch project test): save content đủ 6 element (table + 5 chart) → 200 revision 1, GET round-trip nguyên vẹn; **5 payload sai đều 400** (chartType lạ, bảng lệch cột, donut ratio 0.95, bar thiếu series, bảng 12 cột) và revision không đổi; seed 2 lần liên tiếp ổn định 60 template/60 title unique, **12 template có chart + 5 có table** trong DB.
  - **Test**: `apps/web/scripts/test-charts.ts` (jiti) — 32/32 pass, gồm re-verify 151/151 layout parse schema + layout stats/table chứa element thật + build 2 lần id khác nhau.

- **2026-07-10 — 2 fix theo feedback user (typecheck/lint pass + verify runtime bằng curl trên dev server thật):**
  - **Tab "Yêu thích" ở `/templates`**: bug là chip "Yêu thích" bị GIAO (AND) với tab Hệ thống/Của tôi đang chọn → favorite template hệ thống rồi đứng ở tab "Của tôi" bật lọc sẽ thấy trống (BE trả `isFavorite` đúng — verify curl). Fix: "Yêu thích" thành **tab thứ 3 độc lập** gộp mọi favorite (system + mine), kèm đếm số; empty state riêng. Thêm **optimistic update** cho `useFavoriteTemplate` (sao đổi ngay khi click, rollback nếu lỗi, invalidate khi settled) — trước đó phải đợi refetch xong sao mới đổi.
  - **HomePage theo session**: `app/page.tsx` thành async server component đọc `cookies()` (check nhanh như proxy.ts) — có session hiển thị "Vào Dashboard" + nút "Đăng xuất" (`components/auth/logout-button.tsx`, client, logout xong `router.refresh()` để header đổi về khách); không session giữ nguyên "Đăng nhập"/"Bắt đầu miễn phí".
  - **proxy.ts**: thêm `/login` + `/register` vào matcher với logic guest-only — đã có session gõ 2 url này → redirect `/dashboard` (tôn trọng `?next=` path nội bộ, chặn `//host` open-redirect). Không loop khi cookie stale: refresh fail → BE `clearAuthCookies` → proxy thấy hết cookie. Verify curl đủ 8 kịch bản (có/không session × login/register/dashboard/templates + next hợp lệ + next độc hại).

- **2026-07-10 — Template System: 60 system templates + My Templates CRUD (REQUIREMENTS II) — typecheck/lint/build pass + verify runtime đầy đủ trên DB thật (bộ yêu cầu 2026-07-08 HOÀN THÀNH TOÀN BỘ I→VI):**
  - **60 template = 15 tay có sẵn + 45 SINH TỰ ĐỘNG từ kho 151 layout** (thay vì viết tay từng slide): script tạm `_gen_templates.ts` (chạy qua jiti, đã xóa sau khi dùng) định nghĩa 45 "recipe" `{title, category, layoutIds[]}`, gọi `buildLayoutSlide(id)` ghép 4-6 layout/template, validate `presentationSchema.parse`, xuất ra `apps/api/prisma/seed-templates-generated.ts` (1.3MB, file dữ liệu tĩnh — không hand-edit, sinh lại bằng script nếu cần đổi). `seed.ts` gộp `ALL_SEED_TEMPLATES = [...SEED_TEMPLATES, ...SEED_TEMPLATES_GENERATED]`.
  - **14 category** (mở rộng từ 4): Business(8) Startup(4) Education(5) Portfolio(3) Product(4) Marketing(4) Pitch-Deck(4) Medical(3) Timeline(3) Finance(4) Resume(3) Technology(4) Creative(5) Minimal(5) = 60.
  - **Sự cố phát hiện qua verify**: seed lần đầu ra 59 record — 1 title trùng giữa bộ tay và bộ sinh ("Portfolio cá nhân") khiến 2 nguồn cùng title ghi đè nhau qua cơ chế idempotent-by-title. Đổi tên bản sinh → "Portfolio cá nhân đa năng", seed lại **tự phục hồi** đúng 60 không cần can thiệp DB tay. Verify seed 3 lần liên tiếp ổn định.
  - **Prisma**: bảng mới `TemplateFavorite(userId, templateId)` (migration `add_template_favorite`) — favorite per-user, không đặt cột bool trên `Template`.
  - **Permissions**: thêm `TEMPLATE_CREATE/UPDATE/DELETE` cho role `user`.
  - **BE**: `ProjectsModule` export `ProjectsService` (dùng lại ownership-check có sẵn cho "Lưu làm template"); `TemplatesService`/`Controller` đầy đủ: `listForUser` (gộp system+own, kèm `isFavorite`), `getVisible`, `createFromProject` (Save as Template — đọc content qua `ProjectsService.get()`), `duplicate` (từ BẤT KỲ template thấy được, sinh lại id slide/element), `update`/`remove` (chỉ chủ sở hữu, không đụng được system), `setFavorite` (hoạt động cả trên system lẫn own). Mở rộng `ProjectsService.create()`: tạo project từ `templateId` giờ chấp nhận cả template riêng của chính mình (trước chỉ `isPublic`).
  - **FE**: viết lại trang `/templates` (tab Hệ thống/Của tôi + filter Yêu thích, search, card có Dùng/Nhân bản/★/Sửa/Xóa tuỳ ngữ cảnh); `TemplateRenameDialog`; nút **"Lưu làm template"** trong Toolbar editor (`SaveAsTemplateDialog`); tách `category-meta.ts` (label/màu 14 category) dùng chung 3 nơi.
  - **Verify runtime đầy đủ** (curl + DB Postgres thật, dọn sạch sau cùng): Save as Template từ project thật → đúng tab "Của tôi"; category sai enum → 400; rename/đổi category OK; favorite hoạt động cả trên template của mình lẫn hệ thống; duplicate từ system giữ đúng số slide + id mới; **tạo project từ template riêng thành công** (tính năng mới); **không sửa/xóa được template hệ thống** dù đã login (404); **IDOR đầy đủ 4 endpoint**: user B không thấy/sửa/xóa/xem-chi-tiết/tạo-project-từ template riêng của A (404 tất cả).

- **2026-07-10 — Theme System: 20 system themes + My Themes CRUD (REQUIREMENTS III) — typecheck/lint/build pass + verify runtime đầy đủ trên DB thật:**
  - **Schema** (`themeConfigSchema`, additive optional): `colors.accent2`, `shadow: boolean`, `borderRadius: number`; `colors.background` chấp nhận CSS gradient (convention `includes("gradient(")` giống `ShapeElement.fill`). Thêm `createThemeSchema`/`updateThemeSchema`, `ThemeDto.createdBy`, `LIMITS.THEME_NAME_MAX`.
  - **Permissions**: thêm `THEME_CREATE/UPDATE/DELETE` cho role `user` (giữ `THEME_MANAGE` riêng cho quản trị hệ thống, chưa dùng tới).
  - **Seed 8 → 20 theme** (`seed-themes.ts`): Light, Dark, Corporate, Ocean, Forest, Purple, Orange, Sunset, Pastel, Modern, Material, Minimal, Apple, Google, Gradient, Neon, Elegant, Luxury, Flat, Professional. 8 theme gốc **đổi tên tại chỗ giữ nguyên id** qua cơ chế `legacyNames` trong `seed.ts` (tìm theo tên cũ trước khi tạo mới). Một số theme dùng field mới làm ví dụ thật: Material (shadow+accent2), Gradient (background gradient), Corporate/Google (accent2), Corporate/Elegant/Luxury/Professional (font serif Georgia).
  - **BE `ThemesService`/`ThemesController`**: `listForUser` (system + theme riêng, gộp 1 danh sách cho cả ThemePicker lẫn trang quản lý), `create`, `update`/`remove` (ownership `createdBy=userId AND isSystemTheme=false` — không sửa/xóa được theme hệ thống dù đã đăng nhập), `clone` (nhân bản bất kỳ theme thấy được — hệ thống hoặc của người khác qua share — thành bản riêng mới).
  - **Store `applyTheme` dùng đủ field mới**: `accent2` tô icon tách sắc độ khỏi shape (dùng `accent`), `shadow` bật đổ bóng shape + shadow hướng "bottom" cho ảnh, `borderRadius` áp cho shape rect-family và `image.cornerRadius`, background tự nhận diện gradient.
  - **FE**: trang `/themes` (tab System/My, card swatch — swatch gradient render bằng `backgroundImage` không phải `backgroundColor`, nút Nhân bản/Sửa/Xóa/Xuất JSON/Nhập JSON/Tạo theme); `ThemeEditorDialog` (form đủ field + preview trực tiếp, tái dùng `GRADIENT_PRESETS` tách ra `lib/editor/gradients.ts` dùng chung với `ShapeInspector`); `ThemePicker` (Inspector) thêm badge "· của tôi" + link "Quản lý theme →"; link "Theme" ở dashboard; `proxy.ts` thêm `/themes`.
  - **Verify runtime đầy đủ** (curl + DB Postgres thật, dọn sạch sau cùng): seed 2 lần liên tiếp → đúng 20 record, không trùng tên, không còn tên cũ mồ côi; tạo/sửa/xóa theme riêng OK; clone từ system theme sao chép đúng TOÀN BỘ config (gradient/shadow/borderRadius/accent2 giữ nguyên); **không sửa/xóa được theme hệ thống** dù đã login (404); **IDOR**: user B không thấy/sửa/xóa được theme riêng của A (404 + không lọt list); thiếu field bắt buộc khi tạo → 400 đúng message zod.

- **2026-07-10 — Recycle Bin + cron 90 ngày (REQUIREMENTS IV) — typecheck/lint/build pass + verify runtime đầy đủ trên DB thật:**
  - **BE**: `AssetsModule` export `R2Service` (dùng lại xóa thumbnail); `ProjectsModule` import `AssetsModule`. `ProjectsService` thêm `listTrash` (project `deletedAt != null` của owner), `restore` (404 nếu không ở trong trash), `permanentDelete` (CHỈ cho phép khi project ĐÃ ở trong trash — chặn xóa tắt bỏ qua soft-delete), `purgeProject` dùng chung (xóa thumbnail R2 theo key cố định nếu có Asset + xóa Asset + xóa Project row; lỗi R2 chỉ log warn không chặn xóa DB), `purgeExpiredTrash` chạy `@Cron(EVERY_DAY_AT_MIDNIGHT)` quét `deletedAt < now − LIMITS.TRASH_RETENTION_DAYS` (90, hằng số mới trong `@repo/shared`). Cài `@nestjs/schedule`, đăng ký `ScheduleModule.forRoot()` ở AppModule.
  - **Routes**: `GET /projects/trash` (đặt TRƯỚC `:id` — nếu không Nest match "trash" thành param id), `POST /projects/:id/restore`, `DELETE /projects/:id/permanent` — cả 3 đều `RequirePermission` + ownership check qua `where: { ownerId }`.
  - **FE**: `TrashProjectSummary` type (shared); hooks `useTrashProjects`/`useRestoreProject`/`usePermanentDeleteProject`; trang `/trash` (card thumbnail mờ + badge "Còn N ngày" đỏ khi ≤7, search theo tên, filter "Tất cả/Sắp hết hạn", nút Khôi phục + Xóa vĩnh viễn có confirm, empty state); link "Thùng rác" ở dashboard; `proxy.ts` matcher thêm `/trash`.
  - **Verify runtime ĐẦY ĐỦ** (không chỉ build — chạy `pnpm db:up`, apply migration, boot API thật, curl + script gọi thẳng `NestFactory.createApplicationContext` để test cron): soft-delete↔restore round-trip đúng; permanent-delete chặn khi chưa qua trash (404) và idempotent (404 lần 2); **IDOR**: user B nhận 404 ở cả 3 endpoint khi thao tác project của user A; **cron**: project xóa mềm 91 ngày trước bị purge, xóa mềm 10 ngày trước KHÔNG bị đụng, dữ liệu khác trong DB không ảnh hưởng; project có Asset thumbnail đính kèm: permanent-delete xóa cả row Project lẫn row Asset, không crash dù object R2 không tồn tại thật. Dọn sạch toàn bộ project/user tạo ra trong lúc test.

- **2026-07-09 — Animation System (REQUIREMENTS VI) — test 129/129 + typecheck/lint/build pass:**
  - **Schema** (additive optional): `slide.animations?: Animation[]` + enum `animationGroup/effect/trigger/direction/easing`. 21 hiệu ứng / 4 nhóm (entrance/emphasis/exit/motion).
  - **`lib/editor/animations.ts`**: metadata hiệu ứng, `createAnimation`, **`buildAnimation`** (keyframes+options WAAPI, giữ rotation, autoReverse→iterations×2+alternate, entrance/exit fill forwards), **`playSlideAnimations`** (phát theo bước: with-previous cùng bước, on-click/after-previous đợi bước trước; pre-hide entrance rồi restore; cancel được), default per element type (localStorage).
  - **Store**: `slide.animations` + `addAnimation/updateAnimation/removeAnimation/moveAnimation/reorderAnimations`; `addElement` tự thêm animation mặc định; xoá element/selection dọn animation liên quan. UI store: `animationPanelOpen` (loại trừ layout panel) + `animationClipboard`.
  - **AnimationPanel** (panel phải, nút "Hiệu ứng" toolbar): list theo thứ tự (badge số + màu nhóm), sửa nhanh trigger/duration/delay/direction/repeat/autoReverse/easing, reorder, xoá, **copy/paste sang element khác**, **xem thử từng hiệu ứng** + **"Phát thử slide"**; mục **default animation** theo loại. Badge số thứ tự trên element (canvas) khi mở pane (`data-el-id`+`data-slide-frame`).
  - Test jiti 129 case (schema + keyframes rotation 0/30 + iterations/direction + presentation parse) — 129/129 pass. Preview WAAPI cần test tay browser.

- **2026-07-09 — Icon nền/viền + Ảnh upload thật/bo-góc/shadow + Group đa chọn (REQUIREMENTS I.6/I.7/I.8) — typecheck/lint/build pass + validate 151 layout:**
  - **Schema shared** (đều optional additive, `schemaVersion` giữ 1): `elementBase.groupId`; icon thêm `backgroundColor/backgroundRadius/borderColor/borderWidth`; image thêm `cornerRadius{4 góc}` + `shadow{directions,blur}` + cho `url` nhận `""` (placeholder). Helper render dùng chung `apps/web/src/lib/editor/element-style.ts` (`iconHasBox/iconBoxStyle`, `imageBorderRadius/imageBoxShadow/imageContentStyle`).
  - **I.6 Icon**: Inspector thêm checkbox "Nền phía sau icon" (màu + bo góc) và "Viền none/màu" (màu + độ dày). Render nền/viền cả canvas + preview.
  - **I.7 Ảnh**: tách hook `useUploadImage` (presign→PUT R2→confirm) dùng chung toolbar + Inspector + placeholder. **`imagePh` trong layout giờ sinh image element THẬT url rỗng** → canvas hiện khung "Nhấn đúp để tải ảnh"; Inspector có nút "Tải ảnh/Thay ảnh", bo góc 4 góc riêng (`cornerRadius`), shadow chọn nhiều hướng + blur.
  - **I.8 Group/đa chọn kiểu Canva**: store đổi `selectedElementId`→`selectedElementIds` + `activeGroupId`; actions `selectElement(id,{additive})`, `setSelection`, `enterGroupElement`, `groupSelected`/`ungroupSelected`, `moveElements`, `duplicateSelected`, `removeSelected`. **Ctrl+click** chọn nhiều; **kéo vùng marquee** trên nền chọn element giao vùng; chọn 1 element có group → chọn cả nhóm; **kéo cả nhóm di chuyển sống cùng lúc** (`groupDrag` offset trong `useEditorUiStore`, `onDragMove`); **double-click vào element trong nhóm** để sửa riêng con (không cần bỏ nhóm); nút Nhóm/Bỏ nhóm ở Inspector + phím `Ctrl+G`/`Ctrl+Shift+G`; Delete/Ctrl+D/nudge áp cho cả selection; resize/rotate handle chỉ hiện khi chọn đúng 1 element.
  - Verify: typecheck + lint + build pass; 151 layout vẫn parse `presentationSchema` OK (image placeholder url rỗng hợp lệ). Chưa test tay browser.

- **2026-07-09 — Smart alignment guides (căn chỉnh khi kéo, REQUIREMENTS I.9) — test 14/14 + typecheck/lint/build pass:**
  - **`lib/editor/alignment.ts`** (hàm thuần, có test jiti): `computeAlignment(box, targets, zoom)` so 3 mốc/trục (cạnh đầu / tâm / cạnh cuối) của element đang kéo với mốc các element khác + slide (0 / giữa / hết); chọn lệch nhỏ nhất trong ngưỡng `6px màn hình / zoom`; trả `adjustX/adjustY` (lượng bám) + `vertical/horizontal` (đường guide). Kèm `unionBox` cho kéo nhóm.
  - **Canvas**: `resolveDrag(event)` tính offset đã-snap dùng CHUNG cho `onDragMove` (set `dragGuides` → vẽ guide + dịch element sống) và `onDragEnd` (commit đúng vị trí đã snap) → không nhảy khi thả. Component `AlignmentGuides` vẽ đường nét đứt hồng (#ec4899, dày ~1.5px/zoom, dài hết slide), subscribe `dragGuides` riêng nên Canvas/element khác không re-render theo.
  - **Store UI**: `groupDrag` → `dragGuides {ids,dx,dy,vertical,horizontal}`. **ElementView** chọn primitive `dragGuides.dx/dy` (null nếu không thuộc phần đang kéo) → chỉ element đang kéo re-render; dùng offset đã-snap thay transform thô. Thay thế luôn cơ chế kéo-nhóm-sống cũ (gộp vào dragGuides).
  - Test `computeAlignment` 14 case (snap cạnh/tâm/slide, ngưỡng theo zoom, chọn lệch nhỏ nhất) — 14/14 pass.

- **2026-07-09 (fix + bổ sung) — Marquee chọn vùng + sửa lỗi infinite loop:**
  - **Fix "getSnapshot should be cached"**: selector `useEditorUiStore((s)=>({dx,dy}))` trong `element-view.tsx` trả object MỚI mỗi render → `useSyncExternalStore` lặp vô hạn. Sửa: chọn thẳng `s.groupDrag` (reference ổn định), tính `groupOffset` ngoài selector. (Nguyên tắc: selector zustand phải trả reference ổn định, không tạo object/array literal.)
  - **Marquee**: kéo chuột trên nền slide vẽ vùng chọn (overlay `border-primary`), thả ra chọn mọi element có bounding box GIAO vùng (bỏ element khoá; chạm element có group → chọn cả nhóm qua `setSelection` mở rộng nhóm). Toạ độ logic chia zoom, start giữ ở ref để pointerup không đọc state cũ; kéo <4px = click bỏ chọn. Pointer capture trên khung slide.
  - Verify: typecheck + lint + build pass; grep xác nhận không còn selector nào trả object/array literal.

- **2026-07-09 — REQUIREMENTS mục V: Layout System 151 layout / 18 nhóm — typecheck/lint/build pass + validate schema 151/151:**
  - **DSL layout** `apps/web/src/lib/editor/layouts/helpers.ts`: `slide/text/heading/shape/card/line/connector/icon/imagePh/chip/badge/titleBlock` + bảng màu `C` + font (Inter/serif/mono). Mỗi layout = factory `build(): Slide`, **sinh id slide + element MỚI mỗi lần build** (áp nhiều lần không trùng id); toạ độ logic 1280×720; thuần FE, không đụng DB.
  - **6 file nhóm** (đủ 151): `basic.ts` Title 10 / Quote 4 / List 8 / Column 11; `cards.ts` Card 15 / Comparison 8 / Team 4 / Product 4; `visual.ts` Image 14 / Media 6 / Modern 11; `flow.ts` Timeline 7 / Process 8 / Diagram 10; `data.ts` Statistics 9 / Table 6; `domain.ts` Business 10 / Education 6. Registry `index.ts`: `LAYOUT_GROUPS` (18 nhóm) + `LAYOUT_MAP` + `buildLayoutSlide(id)` + `LAYOUT_COUNT`.
  - **LayoutPanel** (`components/editor/layout-panel.tsx`): drawer trái bật/tắt bằng nút "Bố cục" toolbar (state `useEditorUiStore` mới); search + filter 18 nhóm; mỗi ô thumbnail render THẬT bằng `SlidePreview`; mặc định hiện 1 nhóm để không render đồng thời 151 preview (hiệu năng).
  - **3 cách áp** (store thêm `applyLayoutToSlide` / `addSlideFromLayout` / `applyLayoutToAll`, undo được, tôn trọng slide `locked`): click = áp slide hiện tại (thay bg+elements, giữ id slide + cờ locked/hidden); nút **+** = thêm slide mới; **kéo-thả** vào canvas (MIME `application/x-layout-id`, xử lý trong `canvas.tsx` cùng handler với icon drop); nút **"Áp mọi slide"** theo nhóm (confirm). Áp xong element chỉnh sửa tự do như thường.
  - **Verify**: `pnpm --filter web typecheck/lint/build` pass; script jiti parse toàn bộ 151 layout qua `presentationSchema` → **151/151 OK** (đảm bảo Save không bị 400). Nhân tiện fix latent bug `templates/page.tsx` (thiếu import `TemplateDto`, thay `any`).
  - **Giới hạn**: Statistics/Table dựng chart/bảng bằng placeholder shape/text; Media dùng mockup tĩnh — sẽ thay bằng element thật ở Phase 2c. Chưa test tay browser.

- **2026-07-08 — Cập nhật bộ yêu cầu hệ thống + REQUIREMENTS mục I (fix editor) — typecheck/lint/build pass:**
  - **Docs**: viết `docs/REQUIREMENTS.md` — chốt toàn bộ spec mới I→VI (fix editor / 60 templates + My Templates / 20 themes + My Themes / Recycle Bin 90 ngày / 151 layouts 18 nhóm / Animation system) kèm trạng thái + thiết kế đề xuất cho từng phần; roadmap PROGRESS tách Phase 2b→2f + 3.5.
  - **I.1 Fix sidebar không scroll**: nguyên nhân item flex-col bị `flex-shrink` nén thay vì tràn → thêm `shrink-0` + `min-h-16`; `scroll-smooth`; ảo hóa bằng CSS `content-visibility: auto` + `contain-intrinsic-size` cho danh sách slide dài (100–300 slide).
  - **I.2 Hover actions trên slide** (`slide-panel.tsx`, item đổi button→div role=button vì chứa button con): Nhân bản / Xóa / Lên / Xuống / Khóa / Ẩn — chỉ hiện khi hover, tooltip title, badge 🔒/👁 thường trực; sidebar w-44→w-48. Store thêm `duplicateSlide` (sinh lại id slide + element), `moveSlideBy`, `toggleSlideLocked` (kèm bỏ chọn element), `toggleSlideHidden`. **Slide khóa**: guard trong `addElement` + pointer-events none trên canvas + banner nhắc; **slide ẩn**: cờ `hidden` (trình chiếu Phase 3 sẽ bỏ qua), preview mờ.
  - **I.3 Shape 7 → 63 loại** (`shapeKindSchema` append-only; `shapes.ts` viết lại): hình học sinh bằng helper `polygon/arc/regularPolygon/starPolygon/gearPolygon/heart/cloud`; 6 category (cơ bản 19, mũi tên 10, sao & băng rôn 9, hội thoại 2, biểu tượng 15, flowchart 8); frame/ring/gear đục lỗ bằng subpath ngược chiều (nonzero fill); menu toolbar nhóm theo category + preview `ShapeThumb` vẽ bằng chính clip-path; Inspector: selector 63 hình scroll, **gradient fill** (fill nhận CSS gradient + 8 preset), **viền** (chỉ shape không clip-path — CSS không border được clip-path), **đổ bóng** (prop `shadow`, render `drop-shadow` ở wrapper NGOÀI div bị clip, cả editor + preview), bo góc rect/rounded-rect.
  - **I.4 Icon 20 → 400** (`icon-map.ts` sinh bằng script validate exports lucide-react, giữ key cũ, import tường minh tree-shake, 12 category): **IconPicker** ở toolbar — search realtime, tab category, Favorite (localStorage `vpb:icon-favorites`), Gần đây (`vpb:icon-recent` cap 24), click/double-click chèn, **drag & drop vào canvas** (MIME `application/x-lucide-icon`, toạ độ chia zoom); Inspector icon có search.
  - **I.5 Header/Footer kiểu PowerPoint**: schema shared thêm `presentation.headerFooter` + `slide.hideHeaderFooter` (đều optional, content cũ không vỡ); dialog cấu hình (header/footer/số trang/ngày cố định-hoặc-tự-động/ẩn slide đầu) với 2 nút **Áp dụng tất cả** / **Chỉ slide này** (store `setHeaderFooter(config, scope)`); render đồng nhất qua `HeaderFooterLayer` (canvas + sidebar preview + thumbnail); SlideInspector có toggle ẩn per-slide.
  - Chưa verify tay bằng browser (chỉ typecheck/lint/build) — cần test theo mục "Việc tiếp theo".

- **2026-07-08 — Hoàn thiện editor theo feedback user (trước khi làm Table/Chart):**
  - **Fix sidebar slide list**: trước chỉ hiện ô màu nền trống → giờ render **preview thật thu nhỏ** của slide (tái dùng `SlidePreview`, tự scale theo ResizeObserver), badge số thứ tự nền mờ.
  - **Mở rộng shape 4 → 7 loại**: thêm triangle, diamond, star, arrow (render bằng CSS clip-path — `lib/editor/shapes.ts` dùng chung editor + preview); toolbar đổi 2 nút Khối/Tròn thành **menu "Hình khối"** 7 lựa chọn; Inspector thêm **selector đổi loại hình** cho shape đang chọn; line giờ thêm được từ toolbar.
  - **UX editor**: **Ctrl+D nhân bản** element (+ nút Nhân bản trong Inspector), **mũi tên nudge** 1px (Shift=10px, gộp 1 bước undo cho chuỗi nhấn <800ms), **Escape bỏ chọn**, **auto-fit zoom** theo kích thước màn hình khi mở editor, min-size resize hạ 20 → 6px (line mỏng resize được).
  - Typecheck + lint + build pass.

- **2026-07-08 — Phase 2 đợt 2: Theme system + 15 templates + thumbnail (đã verify API):**
  - **Theme system**: shared `themeConfigSchema` (fontHeading/fontBody + colors background/heading/body/accent, ngưỡng heading = fontSize ≥ 40); BE module `themes/` (GET /themes, THEME_READ); **seed 8 system themes** (Indigo sáng, Dark Slate, Emerald, Sunset, Ocean, Neon Dark, Rose, Mono — validate schema lúc seed, idempotent theo name); store action **`applyTheme`** (đổi themeId + background mọi slide + màu/font text theo heading/body + accent cho shape/icon, image giữ nguyên, undo được); **ThemePicker** trong Inspector (khi không chọn element) hiện swatch 4 màu.
  - **Templates 6 → 15**: thêm Kế hoạch dự án, Giới thiệu công ty, OKR & Mục tiêu (business), Ôn tập trắc nghiệm, Khoa học trực quan (education), Portfolio cá nhân, Mood board (creative), Typography đậm, Timeline đơn giản (minimal).
  - **Thumbnail project**: sau khi Lưu thành công → chụp slide đầu từ bản render ẩn (`SlidePreview` 480px + `html-to-image`) → PUT lên R2 **key cố định** `{userId}/thumbnails/{projectId}.png` (ghi đè, không rác asset; confirm upsert theo key) → PATCH `thumbnailUrl` kèm `?v=` bust cache. Fail im lặng, không chặn luồng lưu. Presign có `purpose: "thumbnail"` (bắt buộc projectId + PNG, thiếu → 400).
  - Verified curl: 8 themes, 15 templates, presign thumbnail trả đúng key cố định, thiếu projectId → 400; typecheck + lint + build pass. **Lưu ý**: capture thumbnail phía browser chưa test tay (cần chạy thật); nếu ảnh R2 trong slide bị CORS khi capture thì thumbnail bỏ qua (silent).

- **2026-07-07 — Fix logic lưu (feedback user) + Phase 2 đợt 1 Templates (đã verify):**
  - **Bỏ autosave → nút Lưu thủ công** + Ctrl+S; indicator "Chưa lưu" (chấm vàng)/"Đang lưu"/"Đã lưu"/"Lưu thất bại"; nút back hỏi confirm khi còn thay đổi chưa lưu; giữ banner 409 + beforeunload.
  - **Fix desync khi mở lại project**: `useProject` staleTime Infinity nhưng save chỉ invalidate list → mở lại editor load bản cache CŨ. Fix: `useSaveProject.onSuccess` **setQueryData `['project', id]`** với content + revision vừa lưu (logic tại `hooks/mutations/useSaveProject.ts`; orchestration lưu tại `hooks/useSavePresentation.ts`).
  - **BE `templates/`**: GET /templates + /templates/:id (isPublic, TEMPLATE_READ, trả kèm content để FE render preview); POST /projects nhận `templateId` → copy content + **sinh lại toàn bộ id slide/element**; templateId sai → 404.
  - **Seed 6 template** (`prisma/seed-templates.ts`, idempotent theo title, **validate `presentationSchema.parse` ngay lúc seed**): Pitch Deck tối giản, Báo cáo kinh doanh (business), Bài giảng sinh động (education), Gradient sáng tạo, Neon Dark (creative), Trắng tối giản (minimal).
  - **FE `/templates`**: gallery nhóm theo category, preview slide đầu render THẬT bằng `SlidePreview` (render tĩnh scale theo container qua ResizeObserver — không cần thumbnail), nút "Dùng" → tạo project → vào editor; dashboard thêm nút "Từ template"; proxy matcher thêm `/templates`.
  - Verified curl: list 6 templates đúng category/số slide; tạo từ template ra project 3 slides/18 elements; typecheck + lint + build pass.

- **2026-07-07 — Phase 1 (đợt 2): Resize/rotate, reorder slide, Inspector, rename:**
  - **Resize 8 hướng + rotate handle** (`selection-handles.tsx`): pointer events + `setPointerCapture`, toạ độ chia zoom, handle giữ kích thước cố định trên màn hình (`size/zoom`), min 20px, Shift khi xoay = snap 15°, **pushHistory đúng 1 lần trước lần mutate đầu** của mỗi thao tác.
  - **Reorder slide** bằng `@dnd-kit/sortable` (thêm dep `@dnd-kit/utilities`), activationConstraint 5px để click chọn không bị nuốt.
  - **Inspector panel** (phải, w-64): text (cỡ/đậm/màu/căn lề/giãn dòng), shape (fill/bo góc), icon (màu + grid đổi icon từ SLIDE_ICONS), image (cover-contain/bo góc); chung: opacity slider, lên trên/xuống dưới (zIndex), khóa/mở khóa, xóa. Không chọn element → chỉnh **màu nền slide** (color picker + 6 preset). History: input pushHistory khi focus/pointerdown (1 snapshot/phiên tương tác).
  - **Rename project từ toolbar**: click title → input → PATCH `/projects/:id` (đã verify curl: rename OK, title rỗng → 400), cache React Query cập nhật cả `['project', id]` lẫn `['projects']`.
  - Store thêm `updateSlide(slideId, updater)`. Typecheck + lint + build pass.

- **2026-07-07 — Phase 1 (đợt 1): Projects + Assets API, Dashboard, Editor cơ bản (đã verify):**
  - **API `projects/`**: CRUD + soft delete; `PUT /projects/:id/content` validate bằng `presentationSchema` + **optimistic concurrency atomic** (`updateMany` có revision trong WHERE — lệch trả 409); ownership check mọi endpoint (verify IDOR: user B đọc/ghi project user A → 404).
  - **API `assets/`**: presign R2 (key `{userId}/{uuid}.{ext}`) → client PUT thẳng lên R2 → confirm (HeadObject verify tồn tại + size/mime thật, không tin client); list; delete (xóa cả object R2 + record). **Verified upload THẬT lên R2**: PUT 200 → public URL 200 → delete → 404.
  - **Web dashboard**: grid project (thumbnail placeholder, ngày sửa), tạo mới → vào editor, xóa (confirm), empty state.
  - **Web editor** (`/editor/[id]`): Zustand store working-copy (load 1 lần, undo/redo snapshot cap 50), canvas DOM 1280×720 scale theo zoom, render đủ 4 element (text/shape/icon/image), **drag bằng dnd-kit** (delta chia zoom, 1 snapshot history/thao tác), click chọn + ring, **double-click sửa text contentEditable**, toolbar (thêm text/khối/tròn/icon/ảnh-upload-R2, undo/redo, zoom, save indicator), **autosave debounce 1.5s kèm revision** → 409 hiện banner "Tải lại phiên bản mới nhất", lỗi mạng tự retry sau 5s, phím tắt Delete/Ctrl+Z/Ctrl+Shift+Z/Ctrl+Y, cảnh báo beforeunload khi chưa lưu; slide panel thêm/xóa/chọn slide.
  - Verified: typecheck + lint + build pass; `/editor/[id]` không cookie → 307 về login, có cookie → 200.

- **2026-07-07 — Phase 0.5: Auth & RBAC (đã verify runtime từng kịch bản bằng curl):**
  - **API `auth/`:** register/login (argon2, lỗi generic chống enumeration), refresh token opaque (SHA-256 hash trong `Session`) **rotation + reuse detection** (dùng lại token cũ → revoke toàn bộ session của user), logout, `GET /auth/me`, Google OAuth (`passport-google-oauth20`, strategy chỉ đăng ký khi env đủ — `GoogleEnabledGuard` trả 404 khi thiếu; merge account theo email).
  - **Cookies:** `access_token` (path=/, JWT 15m nhưng cookie 30d để proxy.ts thấy session), `refresh_token` (path=/auth), httpOnly + sameSite=lax + secure ở prod.
  - **API `rbac/` + `common/`:** `AuthGuard` global (verify JWT + check session revoked trong DB → revoke có tác dụng NGAY), `PermissionsGuard` (cache permission theo role TTL 60s), `@Public()`/`@RequirePermission()`/`@CurrentUser()`, `OriginGuard` global (CSRF), `ZodValidationPipe` (dùng schema @repo/shared), `ThrottlerModule` (5/min register, 10/min login+refresh, 100/min mặc định).
  - **Web:** trang `/login` + `/register` (react-hook-form + zodResolver + schema shared, shadcn Card/Input/Label, nút Google), `/dashboard` (useMe + logout), hooks `useMe`/`useLogin`/`useRegister`/`useLogout`, components ui input/label/card.
  - **Verified bằng curl:** register→me OK; refresh rotate ra token khác; replay token cũ → 401 + toàn bộ session revoke (me bằng access token mới cũng 401 ngay); Origin lạ → 403; logout → me 401; sai mật khẩu → 401 generic; password yếu → 400 với message zod; `/auth/google` → 302 sang accounts.google.com (user đã điền client id/secret thật vào `.env`).

- **2026-07-07 — Phase 0: Scaffold monorepo (đã verify chạy thật):**
  - pnpm workspace: `apps/api`, `apps/web`, `packages/shared`; script root `dev`/`typecheck`/`lint`/`db:up`/`db:migrate`/`db:seed`/`db:studio`.
  - `packages/shared`: zod schemas (`presentation` — discriminated union đủ 4 element Phase 1, `auth`, `project`) + constants (`permissions`, `limits`), build ra `dist/` bằng tsc.
  - `apps/api` NestJS 11: ConfigModule validate env bằng zod (fail-fast), PrismaService global, helmet + cookie-parser + CORS(credentials) + body limit 2MB + ValidationPipe, `GET /health` check DB. **Migration `init` đã áp đủ 10 bảng** (User/Role/Permission/RolePermission/Session/Project/ProjectCollaborator/Asset/Theme/Template). **Seed roles+permissions đã chạy** (idempotent, đọc từ @repo/shared).
  - `apps/web` Next 16: Tailwind v4 + shadcn/ui (components.json, globals.css theme neutral, Button mẫu), lucide-react, React Query provider, **fetch wrapper `src/lib/api/`** (single-flight refresh 401, timeout AbortSignal, ApiError chuẩn hoá), `src/proxy.ts` chặn `/dashboard`+`/editor`.
  - Verified: `pnpm typecheck` + `pnpm lint` pass toàn repo; API `/health` trả `{status:ok,db:up}`; web build + render trang chủ; proxy redirect 307 `/dashboard` → `/login?next=/dashboard`.
- **2026-07-07 — Rà soát & sửa toàn bộ docs (trước khi code):**
  - Chuyển 4 file docs từ root vào `docs/` cho khớp cấu trúc khai báo trong CLAUDE.md.
  - Sửa các thiếu sót: xem Decision Log các dòng ngày 2026-07-07.

## Quyết định kỹ thuật đã chốt (Decision Log)

> Ghi lại mọi quyết định quan trọng để session sau không hỏi lại hoặc làm khác đi.

| Ngày | Quyết định | Lý do |
|---|---|---|
| — | Dùng `proxy.ts` thay `middleware.ts` (Next.js 16) | Convention mới, middleware.ts đã deprecated |
| — | AI Assistant là tính năng làm SAU CÙNG | Ưu tiên trải nghiệm tạo slide + template trước |
| — | Không làm payment/thu phí ở giai đoạn hiện tại | Dự án cá nhân, chưa cần monetize |
| — | Auth dùng cookie httpOnly, không dùng localStorage | Bảo mật cao hơn, chống XSS đánh cắp token |
| 2026-07-07 | **pnpm workspace** monorepo + **packages/shared (@repo/shared)**, zod là source of truth cho type FE–BE | Docs cũ không chốt tooling; 2 bộ validation (zod FE / class-validator BE) sẽ lệch rule |
| 2026-07-07 | Cookie **sameSite=lax** (đổi từ strict) | strict làm mất session khi vào từ link ngoài + vướng OAuth redirect; lax + Origin-check guard vẫn chống CSRF |
| 2026-07-07 | CSRF = **Origin-check guard** tự viết (không csurf/@nestjs/csrf) | csurf deprecated, @nestjs/csrf không tồn tại chính thức |
| 2026-07-07 | Refresh rotation + **reuse detection** (dùng lại token cũ → revoke cả chain) | Rotation không có reuse detection là lỗ hổng phổ biến |
| 2026-07-07 | Permission ↔ Role là **M2M qua RolePermission** (bỏ `roleId` trong Permission) | Thiết kế cũ 1-N làm trùng lặp permission mỗi role |
| 2026-07-07 | Role hệ thống chỉ `admin`/`user`; owner/editor/viewer là **resource-level** check trong service | Trộn 2 tầng quyền vào Role toàn cục là sai mô hình |
| 2026-07-07 | Password hash chốt **argon2** | Bỏ tình trạng "argon2 hoặc bcrypt" nước đôi |
| 2026-07-07 | JSON `Presentation` thêm **`schemaVersion`**; bỏ `Slide.order` (thứ tự = vị trí mảng); `SlideElement` là **discriminated union** ngay từ đầu | Không có version thì sau không migrate được content cũ; order + mảng = 2 nguồn sự thật; props Record<string,unknown> phá strict-mode |
| 2026-07-07 | Project có **`revision`** → save so khớp, lệch trả 409 | Chống mất dữ liệu khi mở 2 tab (last-write-wins âm thầm) |
| 2026-07-07 | BE **validate `content` bằng zod shared + body limit 2MB** trước khi lưu | Không tin JSON từ client; chống payload rác/DoS |
| 2026-07-07 | Thêm bảng **`Asset`** + module `/assets` ngay Phase 1 | Image element cần upload; docs cũ hoàn toàn thiếu phần storage |
| 2026-07-07 | Canvas editor = **DOM-based** (absolute div + CSS transform), KHÔNG Konva/Fabric | Chốt câu hỏi tồn đọng cũ; Phase 1–2 DOM là đủ, text editing dễ hơn |
| 2026-07-07 | FE gọi API qua **fetch wrapper `lib/api/`** (không axios), tự refresh khi 401 | Docs cũ chưa định nghĩa tầng gọi API |
| 2026-07-07 | Storage Asset = **Cloudflare R2** (S3-compatible, cả dev lẫn prod) — credentials trong `apps/api/.env`; upload qua presigned URL | User cung cấp sẵn bucket + credentials; presigned để file không đi qua API server |
| 2026-07-07 | Icon = **lucide-react** (v1.x) | Chuẩn mặc định của shadcn/ui, phổ biến nhất hiện nay, style đồng nhất; `IconElement.props.name` = tên icon lucide |
| 2026-07-07 | UI = **shadcn/ui** (style new-york, base neutral) + **Tailwind CSS v4** | User chốt; component copy vào `src/components/ui/` |
| 2026-07-07 | pnpm 11: approve build scripts qua `allowBuilds` trong `pnpm-workspace.yaml` | pnpm ≥10 chặn postinstall mặc định (prisma, sharp, argon2...) |
| 2026-07-07 | **Bỏ `title` khỏi Presentation JSON** — title chỉ ở cột `Project.title` | Tránh 2 nguồn sự thật; rename không phải ghi lại cả content |
| 2026-07-07 | Icon trên slide: **subset ~20 icon lucide** trong `lib/editor/icon-map.ts`, KHÔNG import động cả bộ | Cả bộ ~1500 icon phình bundle; mở rộng map dần khi cần |
| 2026-07-07 | Upload asset: **presign → PUT thẳng R2 → confirm** (BE HeadObject verify size/mime thật) | File không đi qua API server; không tin metadata client gửi |
| 2026-07-07 | **Lưu THỦ CÔNG (nút Lưu + Ctrl+S), KHÔNG autosave** | User yêu cầu; kèm confirm khi rời trang chưa lưu |
| 2026-07-07 | Save xong phải **setQueryData `['project', id]`** (không chỉ invalidate list) | useProject staleTime Infinity — không sync cache là mở lại thấy bản cũ |
| 2026-07-07 | Tạo project từ template: **copy content + sinh lại id slide/element** | Hai project từ cùng template không được trùng id |
| 2026-07-07 | Preview template = **render tĩnh slide đầu** (`SlidePreview` + ResizeObserver) | Chưa cần pipeline thumbnail; preview luôn đúng 100% nội dung |
| 2026-07-08 | Áp theme = mutate toàn bộ presentation (background + màu/font text theo ngưỡng heading fontSize ≥ 40 + accent shape/icon) | Đơn giản, WYSIWYG, undo được; không làm hệ token phức tạp ở giai đoạn này |
| 2026-07-08 | Thumbnail: **key R2 cố định** `{userId}/thumbnails/{projectId}.png` + `?v=` bust cache | Ghi đè cùng object — không tích rác asset mỗi lần lưu |
| 2026-07-08 | Mọi field mới trong JSON content là **optional additive**, giữ `schemaVersion: 1` (`slide.locked/hidden/hideHeaderFooter`, `presentation.headerFooter`, `shape.shadow`, shape kinds mới append vào enum) | Content cũ parse được nguyên vẹn, không cần migrate |
| 2026-07-08 | Sidebar slide ảo hóa bằng **CSS `content-visibility: auto`** thay vì virtualizer JS | Đủ mượt cho 100–300 slide, không đụng dnd-kit sortable; nếu chưa đủ mới nâng cấp windowing |
| 2026-07-08 | Icon library: **sinh file bằng script validate với exports lucide-react** (400 icon, import tường minh) — KHÔNG import động cả bộ | Giữ tree-shake; tên icon sai bị loại lúc generate thay vì vỡ build |
| 2026-07-08 | Shape shadow render bằng `filter: drop-shadow` trên **wrapper ngoài** div bị clip-path | box-shadow/filter đặt cùng element bị clip sẽ mất bóng |
| 2026-07-08 | Border (stroke) chỉ hỗ trợ shape KHÔNG dùng clip-path | CSS border không áp được lên clip-path; muốn stroke mọi shape phải chuyển render SVG — để mở khi có nhu cầu |
| 2026-07-08 | Header/Footer: config ở `presentation.headerFooter` + cờ ẩn per-slide; scope "Chỉ slide này" = ẩn ở mọi slide khác | Mô hình 1 nguồn config + override đơn giản, khớp hành vi PowerPoint đủ dùng |
| 2026-07-09 | Layout System = **factory FE thuần data** (`lib/editor/layouts/*`) sinh `Slide` placeholder, KHÔNG lưu DB, thumbnail render live bằng `SlidePreview` | 151 layout là bản dựng cố định — data hoá đơn giản, thêm/sửa dễ, không cần bảng/seed; thumbnail luôn khớp 100% |
| 2026-07-09 | Layout `build()` **sinh id slide + element mới mỗi lần gọi**; áp = thay bg+elements nhưng giữ id slide + cờ locked/hidden | Áp nhiều lần không trùng id; giữ id slide để không phá tham chiếu activeSlide/animation sau này |
| 2026-07-09 | Statistics/Table/Media layout dùng **placeholder shape/text** thay chart/bảng/video thật | Chưa có element Chart/Table/Media (Phase 2c); vẫn đủ khung để bố cục, thay element thật sau |
| 2026-07-09 | Placeholder ảnh trong layout = **image element THẬT url rỗng** (không phải icon); schema `image.url` nhận `""` | Cho tải ảnh thật vào đúng khung layout; render khung "Nhấn đúp để tải ảnh" khi rỗng |
| 2026-07-09 | Đa chọn dùng `selectedElementIds: string[]` + `activeGroupId`; nhóm bằng `groupId` trên từng element (không tạo entity group riêng) | Đơn giản, additive vào schema element; chọn/di chuyển/nhân bản theo nhóm suy từ groupId; double-click "vào" group sửa con |
| 2026-07-09 | Kéo nhóm hiển thị sống bằng `groupDrag` offset ở `useEditorUiStore` (onDragMove) thay vì chỉ commit khi thả | dnd-kit chỉ transform element active; cần offset chung để các element cùng chọn dịch chuyển đồng thời như Canva |
| 2026-07-09 | Icon nền/viền + ảnh cornerRadius/shadow: **field optional additive**, render qua `element-style.ts` dùng chung editor+preview | Content cũ không vỡ; editor và preview/thumbnail luôn khớp |
| 2026-07-09 | Smart guides: logic snap tách **hàm thuần `alignment.ts` + test**; hình-ảnh-khi-kéo và vị-trí-lưu dùng chung `resolveDrag` | Tính căn chỉnh là phần "quan trọng, chính xác" → cần test độc lập; snap phải khớp giữa preview và commit để không nhảy khi thả |
| 2026-07-09 | Ngưỡng snap = `6px màn hình / zoom` (không phải hằng số logic) | Cảm giác bám đều ở mọi mức zoom |
| 2026-07-09 | Selector zustand cho drag: chọn **primitive** (dx/dy hoặc null) thay vì object | Chỉ element đang kéo re-render mỗi frame; tránh lỗi "getSnapshot cached" |
| 2026-07-09 | Animation = mảng `slide.animations` (thứ tự = thứ tự phát), render bằng **Web Animations API** (`element.animate`), KHÔNG lib ngoài | Đủ mạnh cho keyframes/timing/easing; preview ngay trong editor; giữ rotation bằng cách ghép `rotate()` vào keyframes |
| 2026-07-09 | Player truy DOM qua `data-slide-frame` + `data-el-id` (không plumbing ref) | Chỉ slide active render → 1 frame duy nhất; đơn giản |
| 2026-07-09 | Default animation lưu **localStorage** (per element type), áp lúc `addElement` | User-preference, không thuộc content presentation |
| 2026-07-09 | Motion path = đường thẳng theo hướng (v1); curve/custom-draw để sau | Đủ "Motion Path (Line)"; custom-draw tốn UI, mở khi cần |
| 2026-07-10 | Recycle Bin: `permanentDelete` CHỈ cho phép khi project đã ở trong trash (không xóa tắt bỏ qua soft-delete) | Bắt buộc luồng 2 bước (xóa mềm → xóa vĩnh viễn) đúng UX Recycle Bin, tránh nhấn nhầm mất dữ liệu |
| 2026-07-10 | Cron purge dùng `@nestjs/schedule` `@Cron(EVERY_DAY_AT_MIDNIGHT)` trong `ProjectsService`, ngưỡng đọc từ `LIMITS.TRASH_RETENTION_DAYS` (shared) | 1 nguồn hằng số cho cả FE hiển thị "còn N ngày" và BE cron — không lệch số |
| 2026-07-10 | `purgeProject` dùng chung cho permanent-delete tay VÀ cron; lỗi xóa R2 chỉ log warn, không chặn xóa DB record | Object R2 mồ côi (hiếm, không nguy hiểm) còn hơn kẹt project không xóa được vì lỗi mạng/R2 tạm thời |
| 2026-07-10 | Route `GET /projects/trash` đặt TRƯỚC `GET /projects/:id` trong controller | Nest match theo thứ tự khai báo — để sau sẽ bị `:id` nuốt mất, trả nhầm 404 "not found" thay vì list trash |
| 2026-07-10 | 8 system theme cũ **đổi tên tại chỗ giữ nguyên id** (cơ chế `legacyNames` tìm theo tên cũ trước khi tạo mới) thay vì xóa-tạo-lại | Giữ id ổn định cho project/template đã tham chiếu `themeId`; xóa-tạo-lại sẽ để lại record mồ côi + đổi id gây vỡ tham chiếu |
| 2026-07-10 | `GET /themes` trả GỘP system + theme riêng của user (không tách 2 endpoint) | 1 danh sách phục vụ cả ThemePicker (editor) lẫn trang quản lý `/themes` (FE tự lọc theo `isSystemTheme`); đơn giản hơn 2 nguồn dữ liệu |
| 2026-07-10 | `PATCH/DELETE /themes/:id` chỉ tác động theme có `createdBy=userId AND isSystemTheme=false` trong WHERE (không check-rồi-throw riêng) | Cùng pattern `updateMany`/`deleteMany` atomic như Projects — 1 query vừa check ownership vừa thực thi, không có khoảng hở race condition |
| 2026-07-10 | accent2 dùng cho ICON, accent (gốc) dùng cho SHAPE khi áp theme | Tách sắc độ giữa 2 loại element cho theme có accent2; theme không khai báo accent2 thì icon dùng lại accent — không phá theme cũ |
| 2026-07-10 | 45 template mới **sinh tự động** bằng cách ghép layout có sẵn (script chạy 1 lần, xuất file dữ liệu tĩnh) thay vì viết tay từng slide | Tái dùng 151 layout đã test/validate kỹ; nhanh hơn nhiều lần so với hand-code ~2000+ dòng vị trí/màu sắc, vẫn đảm bảo qua `presentationSchema.parse` |
| 2026-07-10 | Seed template idempotent theo **title** (không phải id) — khi 2 nguồn dữ liệu trùng title, nguồn sau ghi đè nguồn trước tại cùng 1 row | Phát hiện qua thực nghiệm (xem entry Template System) — bài học: đặt tên phải duy nhất giữa các bộ seed khác nguồn, kể cả khi mỗi bộ tự thân không trùng |
| 2026-07-10 | Favorite template = bảng nối riêng `TemplateFavorite`, KHÔNG phải cột bool trên `Template` | Favorite là trạng thái per-user, không phải thuộc tính của template; cột bool sẽ chỉ đúng cho 1 user |
| 2026-07-10 | "Lưu làm template" đọc content qua `ProjectsService.get()` (không nhận content thô từ client) | Tận dụng lại ownership-check + validate đã có sẵn của Projects, không mở thêm đường tin content tuỳ ý từ client |
| 2026-07-10 | `ProjectsService.create()` từ `templateId` mở rộng nhận cả template **riêng của chính mình** | My Templates vô nghĩa nếu không dùng lại được để tạo project mới |
| 2026-07-10 | Chart render = **SVG thuần tự viết** (`chart-content.tsx` + toán thuần `charts.ts`), KHÔNG dùng chart lib | Nhẹ bundle, render đồng nhất editor/preview/thumbnail, viewBox theo toạ độ logic nên zoom không vỡ; đủ cho bar/line/area/pie/donut |
| 2026-07-10 | `ChartElement.props` = **discriminated union theo `chartType`** (đã chốt từ yêu cầu user); đổi loại chart qua `convertChartType` GIỮ dữ liệu | Mở rộng loại mới chỉ cần append variant; axis↔pie chuyển đổi được không mất số liệu |
| 2026-07-10 | Table = **ma trận `rows: string[][]`** render CSS grid (cột `fr`, hàng `1fr`), sửa ô bằng contentEditable khi double-click | Đơn giản nhất đủ dùng; cell là plain text (React tự escape — không XSS); không cần entity row/col riêng |
| 2026-07-10 | `applyTheme` KHÔNG đổi màu series/lát của chart (chỉ đổi `textColor`); table chỉ ăn `headerBg=accent` | Màu dữ liệu là lựa chọn có chủ đích của user — theme ghi đè sẽ phá phân biệt series |
| 2026-07-10 | Nâng cấp 45 template sinh sẵn bằng **fingerprint text** của slide placeholder (script 1-lần, đã xóa) thay vì viết lại recipe | Script sinh gốc + recipe đã xóa, repo chưa có commit nào để khôi phục; fingerprint + guard idempotent + assert va chạm là đường an toàn nhất |
| 2026-07-10 | **Export CLIENT-SIDE** (html-to-image chụp 2× + jsPDF + pptxgenjs trong browser), KHÔNG dùng Playwright server-side như roadmap phác thảo | Tránh cài chromium (~170MB) lên API + tránh phải mở route render bypass-auth cho headless browser (nhạy cảm bảo mật); tái dùng pipeline thumbnail sẵn có, fidelity 100% với editor. Trade-off: text trong PDF/PPTX là ảnh raster 2× (không copy/sửa được) — nếu cần vector/editable thì nâng cấp server-side sau |
| 2026-07-10 | PPTX = mỗi slide 1 **ảnh full-slide**, không map element sang shape/text native của pptxgenjs | Map 63 shape clip-path + gradient + icon svg sang PPTX native là dự án riêng, dễ lệch fidelity; ảnh đảm bảo đúng 100% những gì thấy trong editor |
| 2026-07-10 | Trình chiếu: player theo trigger (`createPresentationPlayer`) TÁCH RIÊNG khỏi preview player (`playSlideAnimations`) | Preview cần auto-advance toàn bộ để xem thử; trình chiếu cần dừng ĐỢI click ở mỗi bước on-click — trộn 2 semantics vào 1 hàm sẽ rối flag |
| 2026-07-10 | Trình chiếu đánh số trang theo vị trí GỐC của slide (slide ẩn vẫn giữ số) | Khớp hành vi PowerPoint; header/footer "Slide N" không nhảy số khi ẩn/hiện slide |
| 2026-07-10 | Share quyền 3 cấp owner>editor>viewer check trong service (`findAccessible`): không quyền → **404**, viewer cần editor → **403** | 404 chống IDOR (không lộ project tồn tại); 403 chỉ khi user ĐÃ thấy được project — đúng ngữ nghĩa HTTP |
| 2026-07-10 | Collaborator role viewer mở project ở trang `/view/:id` read-only (editor tự redirect), KHÔNG làm chế độ editor-readonly | Editor read-only phải chặn từng tương tác (drag/inspector/phím tắt) — dễ sót lỗ; trang xem riêng tái dùng SlidePreview + Presenter, an toàn tuyệt đối |
| 2026-07-10 | Share public: token `randomBytes(18).base64url` trong cột riêng, bật lại GIỮ token cũ, tắt = null (link chết ngay) | Token đủ entropy không đoán được; giữ token khi bật lại để link đã gửi không vô hiệu oan; route `/p` nằm ngoài proxy matcher |
| 2026-07-10 | `shareToken` chỉ trả cho owner trong ProjectDetail; API public không trả thông tin owner | Editor/viewer không được phép phát tán link public; trang public không lộ email/tên chủ sở hữu |
| 2026-07-11 | Toast tập trung ở `MutationCache` (providers.tsx) với meta `successMessage`/`silentError`, KHÔNG rải toast trong từng component | 1 chỗ duy nhất xử lý lỗi mọi mutation; form/dialog có lỗi inline thì opt-out bằng `silentError` — tránh double message |
| 2026-07-11 | Dashboard preview: thiếu thumbnail thì fetch content qua chính query `['project', id]` (thêm option `enabled` cho `useProject`) và render live `SlidePreview`; hover mới fetch với project đã có thumbnail | Không request N project khi vào trang; cache dùng lại được khi mở editor (staleTime Infinity); mọi project đều có preview kể cả khi thumbnail capture fail |
| 2026-07-11 | `duplicate` project KHÔNG copy `thumbnailUrl` | Key thumbnail R2 cố định theo project GỐC — xóa vĩnh viễn gốc là object bị xóa, bản sao thành link chết; preview live ở dashboard tự hiển thị thay |
| 2026-07-11 | Element media render 3 chế độ (`editor` pointer-events-none / `static` placeholder / `interactive` phát thật) qua prop `interactiveMedia` trên SlideStatic | Editor cần kéo-chọn không vướng iframe; sidebar/preview/export không được nhúng hàng loạt iframe (nặng + html-to-image không chụp được); chỉ trình chiếu/trang xem cần phát thật |
| 2026-07-11 | Embed URL chuẩn hoá phía FE (`toEmbedUrl` — watch/shorts/youtu.be/vimeo → URL nhúng) NGAY khi nhập ở Inspector, lưu URL đã chuẩn hoá | Schema chỉ cần `z.string().url()` đơn giản; render không phải đoán dạng URL; URL lạ giữ nguyên cho phép embed provider khác |
| 2026-07-11 | Giới hạn upload theo LOẠI mime: `maxBytesForMime` (ảnh 10MB, media 100MB) dùng chung schema presign FE-BE + confirm BE check theo mime THẬT từ HeadObject | Video không nhét vừa 10MB; một nguồn số duy nhất, client không lách được vì confirm re-check |
| 2026-07-11 | Media editor: khung media `pointer-events:none`, CHỈ nút play/pause nhận chuột (stopPropagation); embed bấm play mới nạp iframe + nút ✕ trở về | Kéo/chọn element không vướng vùng phát; iframe đang active thì khó kéo (chấp nhận, giống Canva) — ✕ trả lại điều khiển |
| 2026-07-11 | Dọn asset media = reference-count bằng text-search assetId (cuid) trên `content::text` của Project + Template, chạy nền sau save/purge, lỗi chỉ log warn | Project nhân bản/template save-as giữ nguyên assetId → không được xóa file khi còn nơi khác dùng; cuid đủ đặc trưng không dương tính giả; ảnh (image) CHƯA áp cơ chế này — chỉ media theo yêu cầu |
| 2026-07-11 | Quota enforce 2 lớp: presign check theo size khai báo, confirm re-check theo size THẬT (HeadObject) và xóa object nếu vượt; thumbnail MIỄN quota cả 2 lớp | Client khai gian size ở presign sẽ bị chặn ở confirm; thumbnail ghi đè key cố định + nhỏ, tính quota sẽ chặn oan luồng Lưu |
| 2026-07-11 | SUPERADMIN chốt cứng bằng hằng `SUPERADMIN_EMAIL` (shared), tự thăng role admin trong `createSession` (mọi ngả login/register/Google đều qua đây) | Không phụ thuộc seed/đăng ký trước sau; không cần env riêng; các user khác muốn làm admin thì SUPERADMIN đổi role sau (chưa có UI — dùng DB) |
| 2026-07-11 | Khóa user = `lockedAt` + revoke toàn bộ session TRONG TRANSACTION; login check lockedAt SAU khi verify password | Văng ngay mọi thiết bị nhờ AuthGuard check revoked mỗi request (không cần socket); check sau verify để không lộ tài khoản tồn tại qua thứ tự lỗi |

## Vấn đề tồn đọng / Cần quyết định thêm

- Google OAuth callback mới verify được redirect sang Google (302) — luồng callback đầy đủ cần test bằng browser thật khi chạy `pnpm dev`.
- Thumbnail capture (`html-to-image`) khi slide chứa ảnh từ R2: nếu bucket chưa bật CORS cho origin frontend thì canvas bị taint → thumbnail fail (silent). Nếu gặp: cấu hình CORS cho bucket `visual-presentation` trên Cloudflare dashboard (AllowedOrigins: domain FE, AllowedMethods: GET).
- User đã tự chỉnh style trang login/register/landing (gradient, palette tím/cam/xanh trong `globals.css`) + thêm trang `/privacy`, `/terms`, `/cookies` — GIỮ NGUYÊN style này, không revert về theme neutral.
- **PPTX xuất chưa mang animation** (user đã hỏi 2026-07-10): export hiện là ảnh-mỗi-slide → về nguyên tắc KHÔNG THỂ đính animation per-element. Muốn PPTX có hiệu ứng thật cần dự án riêng: map element (text/shape/image/table/chart) sang object native của pptxgenjs + tự sinh XML `p:timing` (pptxgenjs không có API animation) — khối lượng lớn, độ fidelity shape 63 loại/gradient/icon sẽ giảm. Chờ user quyết có làm không.

## Ghi chú cho session tiếp theo

- **Việc tiếp theo**: (a) **test tay bằng browser** (gộp 2026-07-08 + 07-09): sidebar scroll + hover actions, 63 shapes, gradient/shadow/viền, IconPicker, Header/Footer, áp theme, lưu→thumbnail; **Layout** panel "Bố cục" (18 nhóm, click/+/kéo-thả/áp mọi slide, hiệu năng khi bấm "Tất cả"); **Icon nền+viền**; **Ảnh**: tải ảnh vào placeholder layout (nhấn đúp/nút Thay ảnh), bo góc 4 góc, shadow nhiều hướng — **kiểm tra CORS R2 khi ảnh thật hiển thị**; **Group**: Ctrl+click chọn nhiều → Ctrl+G nhóm → kéo cả nhóm (di chuyển sống) → nhấn đúp vào con để sửa riêng → Ctrl+Shift+G bỏ nhóm; **Marquee** kéo vùng nền chọn nhiều; **Alignment guides**: kéo element/nhóm thấy đường nét đứt hồng khi cạnh/tâm khớp element khác hoặc slide + tự bám (thử ở nhiều mức zoom); **Animation**: mở pane "Hiệu ứng" → chọn element → thêm hiệu ứng → chỉnh trigger/timing → "Phát thử slide" (kiểm tra entrance ẩn trước rồi hiện, exit ẩn cuối, with/after-previous, copy/paste, default khi thêm element mới, badge số trên canvas); **Recycle Bin**, **Theme System** và **Template System**: cả ba đã verify runtime đầy đủ bằng curl trên DB thật (xem 3 entry 2026-07-10) — chỉ cần bấm thử UI trên browser: `/trash` (khôi phục/xóa vĩnh viễn), `/themes` (tạo/sửa/xóa/nhân bản/xuất-nhập JSON, áp theme xem accent2/shadow/borderRadius/gradient lên đúng slide), `/templates` (tab Hệ thống/Của tôi, ★ yêu thích, nhân bản, "Lưu làm template" từ nút trong Toolbar editor) — vì logic BE đã chắc chắn đúng; (b) **Phase 2c ĐÃ XONG 2026-07-10** — test tay browser bổ sung: chèn Bảng/Biểu đồ từ toolbar, nhấn đúp sửa ô bảng, ChartInspector (đổi loại giữ dữ liệu, thêm/xóa chuỗi-nhóm-lát, stacked/ngang/smooth/lỗ donut), áp layout Statistics/Table mới, dùng template có chart/table, áp theme lên slide có bảng (header đổi accent). (c) **Phase 3 ĐÃ XONG 2026-07-10 + 3 fix feedback đợt 2 cùng ngày** — test tay browser: nút "Trình chiếu" (mở màn phải ẨN element có entrance on-click, click hiện dần từng bước; ←/chuột phải LÙI đúng 1 bước hiệu ứng, hết bước mới lùi slide và slide trước hiện trạng thái build đủ; transition đặt trong SlideInspector), menu "Xuất" (PDF/PPTX/PNG phải RA NỘI DUNG — đã fix bug trang trắng do chụp wrapper fixed; slide có ảnh R2 cần CORS; kiểm tra luôn thumbnail sau khi Lưu vì fix cùng lỗi), bảng (nhấn đúp → click/kéo chọn vùng ô → tô nền/chữ, thêm/xóa hàng-cột đúng vị trí chọn). (d) **Share ĐÃ XONG 2026-07-10** — test tay: nút "Chia sẻ" trong editor (thêm `shareb@example.com` / `matkhau123` — user test có sẵn trong DB local, đổi role, gỡ), bật link public → mở `/p/<token>` ở cửa sổ ẩn danh, đăng nhập user B xem mục "Được chia sẻ với tôi" ở dashboard (viewer vào `/view`, editor vào editor và lưu được). (e) Việc code tiếp theo = **Phase 4 AI Assistant** — CHƯA có chỉ đạo chi tiết, hỏi user trước.
- **Rà soát tính năng còn thiếu (cập nhật 2026-07-11 đợt 2, ngoài AI)** — (1) trang tài khoản ✅, (2) editor tiện ích ✅, (3) dashboard tìm kiếm/sắp xếp/nhân bản/đổi tên nhanh ✅ XONG, (4) element media ✅ XONG (xem entry 2026-07-11 đợt 2). Còn lại theo ưu tiên: (5) **realtime collaboration** (đồng chỉnh sửa — hiện 2 editor cùng lưu chỉ có revision 409); (6) **PPTX export native** (element thật + animation — xem mục Vấn đề tồn đọng); (7) việc nhỏ: thay 6 layout nhóm Media (đang mockup shape) bằng element media placeholder thật.
- **Test tay cho phần 2026-07-11**: `/account` (đổi tên, upload avatar, đổi mật khẩu → mở tab ẩn danh login trước đó phải văng, thu hồi phiên); editor: chọn element Ctrl+C → chuyển slide → Ctrl+V (nhóm giữ liên kết), Ctrl+X, căn hàng 1 element (theo slide) vs nhiều (theo vùng chọn), "Đều ngang/dọc" với ≥3, kéo góc ảnh (giữ tỉ lệ) + Shift, kéo góc shape + Shift (khóa tỉ lệ).
- **Test tay cho phần 2026-07-11 đợt 3**: editor — chèn video (upload mp4) bấm nút play giữa phát NGAY trong canvas (pause/kéo element vẫn mượt), audio tương tự; embed YouTube hiện thumbnail thật + play → iframe phát, ✕ đóng lại; sidebar slide + dashboard hover: media hiện frame đầu/thumbnail YouTube + badge play (không còn khung đen); xóa element media → Lưu → kiểm tra file biến mất khỏi R2 (bucket dashboard) nếu không project/template nào khác dùng; menu avatar thấy thanh "Dung lượng x/500MB"; upload vượt quota → toast báo nguyên văn; đăng nhập Google bằng danhnh.developer@gmail.com → menu avatar có "Trang quản trị" → /admin: sửa quota inline (MB), khóa user phụ (tab kia văng ngay), mở khóa.
- **Test tay cho phần 2026-07-11 đợt 2**: dashboard — hover project thấy slideshow tự chạy (badge i/n + chấm), project KHÔNG có thumbnail vẫn hiện slide đầu, tìm kiếm/sắp xếp, icon bút chì đổi tên inline, icon copy nhân bản (toast + card mới xuất hiện), xóa → ConfirmDialog đỏ (không còn confirm trình duyệt), menu avatar (Tài khoản/Đăng xuất); toast xuất hiện khi xóa/khôi phục/nhân bản/đổi quyền share/bật-tắt link; editor — menu "Media": chèn Video/Âm thanh/Nhúng → Inspector dán link YouTube dạng watch (tự đổi thành embed), upload mp4/mp3 lên R2, toggles; media trong editor KHÔNG bắt chuột (kéo/chọn bình thường); Trình chiếu → video/iframe phát được, click VÀO media không chuyển slide, click ngoài vẫn chuyển; trang `/view` + `/p/<token>` slide lớn phát media, thumbnail dải dưới là placeholder; export PDF/PNG slide có media ra khung placeholder (đúng thiết kế).
- **QUAN TRỌNG — restart dev server**: API/web `pnpm dev` nếu đang chạy sẽ giữ bản `@repo/shared` CŨ trong RAM (shared vừa build lại thêm schema media + limits). **Phải restart `pnpm dev`** trước khi test, nếu không lưu project chứa media sẽ bị 400 (đã verify bản mới OK trên instance port 3102 riêng — lưu ý có 1 instance API CŨ vẫn đang chiếm port 3101 từ session trước, nên tắt).
- **CẢNH BÁO thao tác shell**: cwd hay bị reset về repo HRM giữa các lệnh — LUÔN `cd /home/hoangdanhdev/Desktop/projects/core/visual-presentation-builder` (đường dẫn tuyệt đối) trong MỖI lệnh Bash, tránh build/seed nhầm repo khác.
- Test bằng browser: `pnpm db:up` → `pnpm dev` → login `test@example.com` / `matkhau123` (DB local có user test, vài project, **60 templates** + **20 themes** đã seed).
- Lưu ý: `apps/api/.env` đã đủ R2 + JWT secrets + Google OAuth keys (user đã điền thật) — đã gitignore. Repo đã `git init` nhưng CHƯA có commit nào.
